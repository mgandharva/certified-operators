apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.22.0
  creationTimestamp: null
  name: containerstoragemodules.storage.dell.com
spec:
  group: storage.dell.com
  names:
    kind: ContainerStorageModule
    listKind: ContainerStorageModuleList
    plural: containerstoragemodules
    shortNames:
      - csm
    singular: containerstoragemodule
  scope: Namespaced
  versions:
    - additionalPrinterColumns:
        - jsonPath: .metadata.creationTimestamp
          name: CreationTime
          type: date
        - description: Type of CSIDriver
          jsonPath: .spec.driver.csiDriverType
          name: CSIDriverType
          type: string
        - description: CSM Version
          jsonPath: .spec.version
          name: Version
          type: string
        - description: State of Installation
          jsonPath: .status.state
          name: State
          type: string
      name: v1
      schema:
        openAPIV3Schema:
          description: ContainerStorageModule is the Schema for the containerstoragemodules
            API
          properties:
            apiVersion:
              description: |-
                APIVersion defines the versioned schema of this representation of an object.
                Servers should convert recognized schemas to the latest internal value, and
                may reject unrecognized values.
                More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
              type: string
            kind:
              description: |-
                Kind is a string value representing the REST resource this object represents.
                Servers may infer this from the endpoint the client submits requests to.
                Cannot be updated.
                In CamelCase.
                More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
              type: string
            metadata:
              type: object
            spec:
              description: ContainerStorageModuleSpec defines the desired state of
                ContainerStorageModule
              properties:
                customRegistry:
                  description: CustomRegistry is the custom registry for the image
                  type: string
                driver:
                  description: Driver is a CSI Drivers for Dell Technologies
                  maxProperties: 20
                  properties:
                    authSecret:
                      description: AuthSecret is the name of the credentials secret
                        for the driver
                      type: string
                    common:
                      description: Common is the common specification for both controller
                        and node plugins
                      properties:
                        args:
                          description: Args is the set of arguments for the container
                          items:
                            type: string
                          type: array
                        authorizationController:
                          description: AuthorizationController is the image tag for
                            the container
                          type: string
                        authorizationControllerReplicas:
                          description: AuthorizationControllerReplicas is the number
                            of replicas for the authorization controller deployment
                          type: integer
                        certificate:
                          description: Certificate is a certificate used for a certificate/private-key
                            pair
                          type: string
                        certificateAuthority:
                          description: CertificateAuthority is a certificate authority
                            used to validate a certificate
                          type: string
                        commander:
                          description: Commander is the image tag for the Container
                          type: string
                        configSecretProviderClass:
                          description: |-
                            ConfigSecretProviderClass is the SecretProviderClass Object details for config secret
                            Applicable from CSM v1.15 onwards
                          items:
                            description: ConfigSecretProviderClass is the config secret
                              configuration for CSM Authorization
                            properties:
                              configSecretName:
                                description: ConfigSecretName is the name of the Kubernetes
                                  secret created by the CSI driver
                                type: string
                              conjur:
                                description: Conjur is the secret configuration with
                                  path to retrieve the Config secret from Conjur
                                properties:
                                  secretPath:
                                    description: SecretPath is the path to the config
                                      secret
                                    type: string
                                type: object
                              secretProviderClassName:
                                description: SecretProviderClassName is the name of
                                  the SecretProviderClass that holds the config secretObject
                                type: string
                            type: object
                          maxItems: 1
                          type: array
                        controllerReconcileInterval:
                          description: The interval which the reconcile of each controller
                            is run
                          type: string
                        enabled:
                          description: Enabled is used to indicate wether or not to
                            deploy a module
                          type: boolean
                        envs:
                          description: Envs is the set of environment variables for
                            the container
                          items:
                            description: EnvVar represents an environment variable
                              present in a Container.
                            properties:
                              name:
                                description: |-
                                  Name of the environment variable.
                                  May consist of any printable ASCII characters except '='.
                                type: string
                              value:
                                description: |-
                                  Variable references $(VAR_NAME) are expanded
                                  using the previously defined environment variables in the container and
                                  any service environment variables. If a variable cannot be resolved,
                                  the reference in the input string will be unchanged. Double $$ are reduced
                                  to a single $, which allows for escaping the $(VAR_NAME) syntax: i.e.
                                  "$$(VAR_NAME)" will produce the string literal "$(VAR_NAME)".
                                  Escaped references will never be expanded, regardless of whether the variable
                                  exists or not.
                                  Defaults to "".
                                type: string
                              valueFrom:
                                description: Source for the environment variable's
                                  value. Cannot be used if value is not empty.
                                properties:
                                  configMapKeyRef:
                                    description: Selects a key of a ConfigMap.
                                    properties:
                                      key:
                                        description: The key to select.
                                        type: string
                                      name:
                                        default: ""
                                        description: |-
                                          Name of the referent.
                                          This field is effectively required, but due to backwards compatibility is
                                          allowed to be empty. Instances of this type with an empty value here are
                                          almost certainly wrong.
                                          More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                        type: string
                                      optional:
                                        description: Specify whether the ConfigMap
                                          or its key must be defined
                                        type: boolean
                                    required:
                                      - key
                                    type: object
                                    x-kubernetes-map-type: atomic
                                  fieldRef:
                                    description: |-
                                      Selects a field of the pod: supports metadata.name, metadata.namespace, `metadata.labels['<KEY>']`, `metadata.annotations['<KEY>']`,
                                      spec.nodeName, spec.serviceAccountName, status.hostIP, status.podIP, status.podIPs.
                                    properties:
                                      apiVersion:
                                        description: Version of the schema the FieldPath
                                          is written in terms of, defaults to "v1".
                                        type: string
                                      fieldPath:
                                        description: Path of the field to select in
                                          the specified API version.
                                        type: string
                                    required:
                                      - fieldPath
                                    type: object
                                    x-kubernetes-map-type: atomic
                                  fileKeyRef:
                                    description: |-
                                      FileKeyRef selects a key of the env file.
                                      Requires the EnvFiles feature gate to be enabled.
                                    properties:
                                      key:
                                        description: |-
                                          The key within the env file. An invalid key will prevent the pod from starting.
                                          The keys defined within a source may consist of any printable ASCII characters except '='.
                                          During Alpha stage of the EnvFiles feature gate, the key size is limited to 128 characters.
                                        type: string
                                      optional:
                                        default: false
                                        description: |-
                                          Specify whether the file or its key must be defined. If the file or key
                                          does not exist, then the env var is not published.
                                          If optional is set to true and the specified key does not exist,
                                          the environment variable will not be set in the Pod's containers.

                                          If optional is set to false and the specified key does not exist,
                                          an error will be returned during Pod creation.
                                        type: boolean
                                      path:
                                        description: |-
                                          The path within the volume from which to select the file.
                                          Must be relative and may not contain the '..' path or start with '..'.
                                        type: string
                                      volumeName:
                                        description: The name of the volume mount
                                          containing the env file.
                                        type: string
                                    required:
                                      - key
                                      - path
                                      - volumeName
                                    type: object
                                    x-kubernetes-map-type: atomic
                                  resourceFieldRef:
                                    description: |-
                                      Selects a resource of the container: only resources limits and requests
                                      (limits.cpu, limits.memory, limits.ephemeral-storage, requests.cpu, requests.memory and requests.ephemeral-storage) are currently supported.
                                    properties:
                                      containerName:
                                        description: "Container name: required for
                                          volumes, optional for env vars"
                                        type: string
                                      divisor:
                                        anyOf:
                                          - type: integer
                                          - type: string
                                        description: Specifies the output format of
                                          the exposed resources, defaults to "1"
                                        pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                        x-kubernetes-int-or-string: true
                                      resource:
                                        description: "Required: resource to select"
                                        type: string
                                    required:
                                      - resource
                                    type: object
                                    x-kubernetes-map-type: atomic
                                  secretKeyRef:
                                    description: Selects a key of a secret in the
                                      pod's namespace
                                    properties:
                                      key:
                                        description: The key of the secret to select
                                          from.  Must be a valid secret key.
                                        type: string
                                      name:
                                        default: ""
                                        description: |-
                                          Name of the referent.
                                          This field is effectively required, but due to backwards compatibility is
                                          allowed to be empty. Instances of this type with an empty value here are
                                          almost certainly wrong.
                                          More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                        type: string
                                      optional:
                                        description: Specify whether the Secret or
                                          its key must be defined
                                        type: boolean
                                    required:
                                      - key
                                    type: object
                                    x-kubernetes-map-type: atomic
                                type: object
                            required:
                              - name
                            type: object
                          maxItems: 30
                          type: array
                        gateway:
                          description: Gateway is the gateway configuration for the
                            authorization proxy-server (v2.5.0+)
                          properties:
                            annotations:
                              additionalProperties:
                                type: string
                              description: Annotations is an unstructured key value
                                map that stores additional annotations for the proxy-server
                                HTTPRoute
                              type: object
                            gatewayClassName:
                              description: GatewayClassName is the name of the GatewayClass
                                for the proxy-server Gateway resource
                              type: string
                            hosts:
                              description: Hosts is the additional hostnames for the
                                proxy-server HTTPRoute
                              items:
                                type: string
                              type: array
                          type: object
                        hostname:
                          description: Hostname is the authorization proxy server
                            hostname
                          type: string
                        image:
                          description: Image is the image tag for the Container
                          type: string
                        imagePullPolicy:
                          description: ImagePullPolicy is the image pull policy for
                            the image
                          type: string
                        leaderElection:
                          description: LeaderElection is boolean flag to enable leader
                            election
                          type: boolean
                        name:
                          description: Name is the name of Container
                          type: string
                        nodeSelector:
                          additionalProperties:
                            type: string
                          description: |-
                            NodeSelector is a selector which must be true for the pod to fit on a node.
                            Selector which must match a node's labels for the pod to be scheduled on that node.
                          type: object
                        opa:
                          description: Opa is the image tag for the Container
                          type: string
                        opaKubeMgmt:
                          description: OpaKubeMgmt is the image tag for the Container
                          type: string
                        openTelemetryCollectorAddress:
                          description: OpenTelemetryCollectorAddress is the address
                            of the OTLP receiving endpoint using gRPC
                          type: string
                        ports:
                          description: Ports is the list of ports for the container
                          items:
                            description: ContainerPort represents a network port in
                              a single container.
                            properties:
                              containerPort:
                                description: |-
                                  Number of port to expose on the pod's IP address.
                                  This must be a valid port number, 0 < x < 65536.
                                format: int32
                                type: integer
                              hostIP:
                                description: What host IP to bind the external port
                                  to.
                                type: string
                              hostPort:
                                description: |-
                                  Number of port to expose on the host.
                                  If specified, this must be a valid port number, 0 < x < 65536.
                                  If HostNetwork is specified, this must match ContainerPort.
                                  Most containers do not need this.
                                format: int32
                                type: integer
                              name:
                                description: |-
                                  If specified, this must be an IANA_SVC_NAME and unique within the pod. Each
                                  named port in a pod must have a unique name. Name for the port that can be
                                  referred to by services.
                                type: string
                              protocol:
                                default: TCP
                                description: |-
                                  Protocol for port. Must be UDP, TCP, or SCTP.
                                  Defaults to "TCP".
                                type: string
                            required:
                              - containerPort
                            type: object
                          type: array
                        privateKey:
                          description: PrivateKey is a private key used for a certificate/private-key
                            pair
                          type: string
                        proxyServerIngress:
                          description: ProxyServerIngress is the authorization proxy
                            server ingress configuration
                          items:
                            description: ProxyServerIngress is the authorization ingress
                              configuration struct
                            properties:
                              annotations:
                                additionalProperties:
                                  type: string
                                description: Annotations is an unstructured key value
                                  map that stores additional annotations for the ingress
                                type: object
                              hosts:
                                description: Hosts is the hosts rules for the ingress
                                items:
                                  type: string
                                type: array
                              ingressClassName:
                                description: IngressClassName is the ingressClassName
                                type: string
                            type: object
                          type: array
                        proxyService:
                          description: ProxyService is the image tag for the Container
                          type: string
                        proxyServiceReplicas:
                          description: ProxyServiceReplicas is the number of replicas
                            for the proxy service deployment
                          type: integer
                        redis:
                          description: Redis is the image tag for the Container
                          type: string
                        redisCommander:
                          description: RedisCommander is the name of the redis deployment
                          type: string
                        redisName:
                          description: RedisName is the name of the redis statefulset
                          type: string
                        redisPassword:
                          description: RedisPassword is the password for the redis
                            instance
                          type: string
                        redisReplicas:
                          description: RedisReplicas is the number of replicas for
                            the redis deployment
                          type: integer
                        redisSecretProviderClass:
                          description: |-
                            RedisSecretProviderClass is the SecretProviderClass Object details for redis
                            Applicable from CSM v1.15 onwards
                          items:
                            description: RedisSecretProviderClass is the redis secret
                              configuration for CSM Authorization
                            properties:
                              conjur:
                                description: Conjur is the secret configuration with
                                  path to retrieve the Redis credentials from Conjur
                                properties:
                                  passwordPath:
                                    description: PasswordPath is the path to the password
                                      in the secret
                                    type: string
                                  usernamePath:
                                    description: UsernamePath is the path to the username
                                      in the secret
                                    type: string
                                type: object
                              redisPasswordKey:
                                description: RedisPasswordKey is the key in the secret
                                  that holds the Redis password
                                type: string
                              redisSecretName:
                                description: RedisSecretName is the name of the Kubernetes
                                  secret created by the CSI driver
                                type: string
                              redisUsernameKey:
                                description: RedisUsernameKey is the key in the secret
                                  that holds the Redis username
                                type: string
                              secretProviderClassName:
                                description: SecretProviderClassName is the name of
                                  the SecretProviderClass that holds the Redis secretObject
                                type: string
                            type: object
                          maxItems: 1
                          type: array
                        redisUsername:
                          description: RedisUsername is the username for the redis
                            instance
                          type: string
                        roleService:
                          description: RoleService is the image tag for the Container
                          type: string
                        roleServiceReplicas:
                          description: RoleServiceReplicas is the number of replicas
                            for the role service deployment
                          type: integer
                        secretProviderClasses:
                          description: |-
                            SecretProviderClasses is a collection of secret provider classes for retrieving secrets from external providers for storage system credentials
                            Applicable from CSM v1.15 onwards
                            Only one of SecretProviderClasses or Secrets must be specified (mutually exclusive)
                          properties:
                            conjur:
                              description: Conjur is the list SecretProviderClass
                                names provided by Conjur
                              items:
                                properties:
                                  name:
                                    description: Name is the name of the Conjur SecretProviderClass
                                    type: string
                                  paths:
                                    description: Paths is the list of paths to the
                                      secrets in Conjur
                                    items:
                                      properties:
                                        passwordPath:
                                          description: PasswordPath is the path to
                                            the password in the secret
                                          type: string
                                        usernamePath:
                                          description: UsernamePath is the path to
                                            the username in the secret
                                          type: string
                                      type: object
                                    type: array
                                type: object
                              type: array
                            vault:
                              description: Vault is the list SecretProviderClass names
                                provided by Vault
                              items:
                                type: string
                              type: array
                          type: object
                        secrets:
                          description: |-
                            Secrets is a collection of kubernetes secrets for storage system credentials
                            Applicable from CSM v1.15 onwards
                            Only one of SecretProviderClasses or Secrets must be specified (mutually exclusive)
                          items:
                            type: string
                          type: array
                        sentinel:
                          description: Sentinel is the name of the sentinel statefulSet
                          type: string
                        skipCertificateValidation:
                          description: skipCertificateValidation is the flag to skip
                            certificate validation
                          type: boolean
                        storageService:
                          description: StorageService is the image tag for the Container
                          type: string
                        storageServiceReplicas:
                          description: StorageServiceReplicas is the number of replicas
                            for storage service deployment
                          type: integer
                        tenantService:
                          description: TenantService is the image tag for the Container
                          type: string
                        tenantServiceReplicas:
                          description: TenantServiceReplicas is the number of replicas
                            for the tenant service deployment
                          type: integer
                        tolerations:
                          description: Tolerations is the list of tolerations for
                            the driver pods
                          items:
                            description: |-
                              The pod this Toleration is attached to tolerates any taint that matches
                              the triple <key,value,effect> using the matching operator <operator>.
                            properties:
                              effect:
                                description: |-
                                  Effect indicates the taint effect to match. Empty means match all taint effects.
                                  When specified, allowed values are NoSchedule, PreferNoSchedule and NoExecute.
                                type: string
                              key:
                                description: |-
                                  Key is the taint key that the toleration applies to. Empty means match all taint keys.
                                  If the key is empty, operator must be Exists; this combination means to match all values and all keys.
                                type: string
                              operator:
                                description: |-
                                  Operator represents a key's relationship to the value.
                                  Valid operators are Exists, Equal, Lt, and Gt. Defaults to Equal.
                                  Exists is equivalent to wildcard for value, so that a pod can
                                  tolerate all taints of a particular category.
                                  Lt and Gt perform numeric comparisons (requires feature gate TaintTolerationComparisonOperators).
                                type: string
                              tolerationSeconds:
                                description: |-
                                  TolerationSeconds represents the period of time the toleration (which must be
                                  of effect NoExecute, otherwise this field is ignored) tolerates the taint. By default,
                                  it is not set, which means tolerate the taint forever (do not evict). Zero and
                                  negative values will be treated as 0 (evict immediately) by the system.
                                format: int64
                                type: integer
                              value:
                                description: |-
                                  Value is the taint value the toleration matches to.
                                  If the operator is Exists, the value should be empty, otherwise just a regular string.
                                type: string
                            type: object
                          type: array
                        vaultConfigurations:
                          description: |-
                            Vaults are the vault configurations
                            Applicable till CSM v1.14
                          items:
                            description: Vault is the configuration for a vault instance
                              struct
                            properties:
                              address:
                                description: Address is the address for this vault
                                type: string
                              certificateAuthority:
                                description: CertificateAuthority is the base64-encoded
                                  certificate authority for validaitng the vault certificate
                                type: string
                              clientCertificate:
                                description: ClientCertificate is the base64-encoded
                                  certificate for connecting to vault
                                type: string
                              clientKey:
                                description: ClientKey validates is the base64-encoded
                                  certificate key for connecting to vault
                                type: string
                              identifier:
                                description: Identifier is the identifier for this
                                  vault
                                type: string
                              role:
                                description: Role is the role for this vault
                                type: string
                              skipCertificateValidation:
                                description: SkipCertificateValidation validates the
                                  vault server certificate or not
                                type: boolean
                            type: object
                          type: array
                        volumeMounts:
                          description: VolumeMounts is the list of volume mounts for
                            the container
                          items:
                            description: VolumeMount describes a mounting of a Volume
                              within a container.
                            properties:
                              mountPath:
                                description: |-
                                  Path within the container at which the volume should be mounted.  Must
                                  not contain ':'.
                                type: string
                              mountPropagation:
                                description: |-
                                  mountPropagation determines how mounts are propagated from the host
                                  to container and the other way around.
                                  When not set, MountPropagationNone is used.
                                  This field is beta in 1.10.
                                  When RecursiveReadOnly is set to IfPossible or to Enabled, MountPropagation must be None or unspecified
                                  (which defaults to None).
                                type: string
                              name:
                                description: This must match the Name of a Volume.
                                type: string
                              readOnly:
                                description: |-
                                  Mounted read-only if true, read-write otherwise (false or unspecified).
                                  Defaults to false.
                                type: boolean
                              recursiveReadOnly:
                                description: |-
                                  RecursiveReadOnly specifies whether read-only mounts should be handled
                                  recursively.

                                  If ReadOnly is false, this field has no meaning and must be unspecified.

                                  If ReadOnly is true, and this field is set to Disabled, the mount is not made
                                  recursively read-only.  If this field is set to IfPossible, the mount is made
                                  recursively read-only, if it is supported by the container runtime.  If this
                                  field is set to Enabled, the mount is made recursively read-only if it is
                                  supported by the container runtime, otherwise the pod will not be started and
                                  an error will be generated to indicate the reason.

                                  If this field is set to IfPossible or Enabled, MountPropagation must be set to
                                  None (or be unspecified, which defaults to None).

                                  If this field is not specified, it is treated as an equivalent of Disabled.
                                type: string
                              subPath:
                                description: |-
                                  Path within the volume from which the container's volume should be mounted.
                                  Defaults to "" (volume's root).
                                type: string
                              subPathExpr:
                                description: |-
                                  Expanded path within the volume from which the container's volume should be mounted.
                                  Behaves similarly to SubPath but environment variable references $(VAR_NAME) are expanded using the container's environment.
                                  Defaults to "" (volume's root).
                                  SubPathExpr and SubPath are mutually exclusive.
                                type: string
                            required:
                              - mountPath
                              - name
                            type: object
                          type: array
                      type: object
                    configVersion:
                      description: |-
                        ConfigVersion is the configuration version of the driver
                        Deprecated: Use spec.version instead. This field will be removed in a future release.
                      type: string
                    controller:
                      description: Controller is the specification for Controller
                        plugin only
                      properties:
                        args:
                          description: Args is the set of arguments for the container
                          items:
                            type: string
                          type: array
                        authorizationController:
                          description: AuthorizationController is the image tag for
                            the container
                          type: string
                        authorizationControllerReplicas:
                          description: AuthorizationControllerReplicas is the number
                            of replicas for the authorization controller deployment
                          type: integer
                        certificate:
                          description: Certificate is a certificate used for a certificate/private-key
                            pair
                          type: string
                        certificateAuthority:
                          description: CertificateAuthority is a certificate authority
                            used to validate a certificate
                          type: string
                        commander:
                          description: Commander is the image tag for the Container
                          type: string
                        configSecretProviderClass:
                          description: |-
                            ConfigSecretProviderClass is the SecretProviderClass Object details for config secret
                            Applicable from CSM v1.15 onwards
                          items:
                            description: ConfigSecretProviderClass is the config secret
                              configuration for CSM Authorization
                            properties:
                              configSecretName:
                                description: ConfigSecretName is the name of the Kubernetes
                                  secret created by the CSI driver
                                type: string
                              conjur:
                                description: Conjur is the secret configuration with
                                  path to retrieve the Config secret from Conjur
                                properties:
                                  secretPath:
                                    description: SecretPath is the path to the config
                                      secret
                                    type: string
                                type: object
                              secretProviderClassName:
                                description: SecretProviderClassName is the name of
                                  the SecretProviderClass that holds the config secretObject
                                type: string
                            type: object
                          maxItems: 1
                          type: array
                        controllerReconcileInterval:
                          description: The interval which the reconcile of each controller
                            is run
                          type: string
                        enabled:
                          description: Enabled is used to indicate wether or not to
                            deploy a module
                          type: boolean
                        envs:
                          description: Envs is the set of environment variables for
                            the container
                          items:
                            description: EnvVar represents an environment variable
                              present in a Container.
                            properties:
                              name:
                                description: |-
                                  Name of the environment variable.
                                  May consist of any printable ASCII characters except '='.
                                type: string
                              value:
                                description: |-
                                  Variable references $(VAR_NAME) are expanded
                                  using the previously defined environment variables in the container and
                                  any service environment variables. If a variable cannot be resolved,
                                  the reference in the input string will be unchanged. Double $$ are reduced
                                  to a single $, which allows for escaping the $(VAR_NAME) syntax: i.e.
                                  "$$(VAR_NAME)" will produce the string literal "$(VAR_NAME)".
                                  Escaped references will never be expanded, regardless of whether the variable
                                  exists or not.
                                  Defaults to "".
                                type: string
                              valueFrom:
                                description: Source for the environment variable's
                                  value. Cannot be used if value is not empty.
                                properties:
                                  configMapKeyRef:
                                    description: Selects a key of a ConfigMap.
                                    properties:
                                      key:
                                        description: The key to select.
                                        type: string
                                      name:
                                        default: ""
                                        description: |-
                                          Name of the referent.
                                          This field is effectively required, but due to backwards compatibility is
                                          allowed to be empty. Instances of this type with an empty value here are
                                          almost certainly wrong.
                                          More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                        type: string
                                      optional:
                                        description: Specify whether the ConfigMap
                                          or its key must be defined
                                        type: boolean
                                    required:
                                      - key
                                    type: object
                                    x-kubernetes-map-type: atomic
                                  fieldRef:
                                    description: |-
                                      Selects a field of the pod: supports metadata.name, metadata.namespace, `metadata.labels['<KEY>']`, `metadata.annotations['<KEY>']`,
                                      spec.nodeName, spec.serviceAccountName, status.hostIP, status.podIP, status.podIPs.
                                    properties:
                                      apiVersion:
                                        description: Version of the schema the FieldPath
                                          is written in terms of, defaults to "v1".
                                        type: string
                                      fieldPath:
                                        description: Path of the field to select in
                                          the specified API version.
                                        type: string
                                    required:
                                      - fieldPath
                                    type: object
                                    x-kubernetes-map-type: atomic
                                  fileKeyRef:
                                    description: |-
                                      FileKeyRef selects a key of the env file.
                                      Requires the EnvFiles feature gate to be enabled.
                                    properties:
                                      key:
                                        description: |-
                                          The key within the env file. An invalid key will prevent the pod from starting.
                                          The keys defined within a source may consist of any printable ASCII characters except '='.
                                          During Alpha stage of the EnvFiles feature gate, the key size is limited to 128 characters.
                                        type: string
                                      optional:
                                        default: false
                                        description: |-
                                          Specify whether the file or its key must be defined. If the file or key
                                          does not exist, then the env var is not published.
                                          If optional is set to true and the specified key does not exist,
                                          the environment variable will not be set in the Pod's containers.

                                          If optional is set to false and the specified key does not exist,
                                          an error will be returned during Pod creation.
                                        type: boolean
                                      path:
                                        description: |-
                                          The path within the volume from which to select the file.
                                          Must be relative and may not contain the '..' path or start with '..'.
                                        type: string
                                      volumeName:
                                        description: The name of the volume mount
                                          containing the env file.
                                        type: string
                                    required:
                                      - key
                                      - path
                                      - volumeName
                                    type: object
                                    x-kubernetes-map-type: atomic
                                  resourceFieldRef:
                                    description: |-
                                      Selects a resource of the container: only resources limits and requests
                                      (limits.cpu, limits.memory, limits.ephemeral-storage, requests.cpu, requests.memory and requests.ephemeral-storage) are currently supported.
                                    properties:
                                      containerName:
                                        description: "Container name: required for
                                          volumes, optional for env vars"
                                        type: string
                                      divisor:
                                        anyOf:
                                          - type: integer
                                          - type: string
                                        description: Specifies the output format of
                                          the exposed resources, defaults to "1"
                                        pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                        x-kubernetes-int-or-string: true
                                      resource:
                                        description: "Required: resource to select"
                                        type: string
                                    required:
                                      - resource
                                    type: object
                                    x-kubernetes-map-type: atomic
                                  secretKeyRef:
                                    description: Selects a key of a secret in the
                                      pod's namespace
                                    properties:
                                      key:
                                        description: The key of the secret to select
                                          from.  Must be a valid secret key.
                                        type: string
                                      name:
                                        default: ""
                                        description: |-
                                          Name of the referent.
                                          This field is effectively required, but due to backwards compatibility is
                                          allowed to be empty. Instances of this type with an empty value here are
                                          almost certainly wrong.
                                          More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                        type: string
                                      optional:
                                        description: Specify whether the Secret or
                                          its key must be defined
                                        type: boolean
                                    required:
                                      - key
                                    type: object
                                    x-kubernetes-map-type: atomic
                                type: object
                            required:
                              - name
                            type: object
                          maxItems: 30
                          type: array
                        gateway:
                          description: Gateway is the gateway configuration for the
                            authorization proxy-server (v2.5.0+)
                          properties:
                            annotations:
                              additionalProperties:
                                type: string
                              description: Annotations is an unstructured key value
                                map that stores additional annotations for the proxy-server
                                HTTPRoute
                              type: object
                            gatewayClassName:
                              description: GatewayClassName is the name of the GatewayClass
                                for the proxy-server Gateway resource
                              type: string
                            hosts:
                              description: Hosts is the additional hostnames for the
                                proxy-server HTTPRoute
                              items:
                                type: string
                              type: array
                          type: object
                        hostname:
                          description: Hostname is the authorization proxy server
                            hostname
                          type: string
                        image:
                          description: Image is the image tag for the Container
                          type: string
                        imagePullPolicy:
                          description: ImagePullPolicy is the image pull policy for
                            the image
                          type: string
                        leaderElection:
                          description: LeaderElection is boolean flag to enable leader
                            election
                          type: boolean
                        name:
                          description: Name is the name of Container
                          type: string
                        nodeSelector:
                          additionalProperties:
                            type: string
                          description: |-
                            NodeSelector is a selector which must be true for the pod to fit on a node.
                            Selector which must match a node's labels for the pod to be scheduled on that node.
                          type: object
                        opa:
                          description: Opa is the image tag for the Container
                          type: string
                        opaKubeMgmt:
                          description: OpaKubeMgmt is the image tag for the Container
                          type: string
                        openTelemetryCollectorAddress:
                          description: OpenTelemetryCollectorAddress is the address
                            of the OTLP receiving endpoint using gRPC
                          type: string
                        ports:
                          description: Ports is the list of ports for the container
                          items:
                            description: ContainerPort represents a network port in
                              a single container.
                            properties:
                              containerPort:
                                description: |-
                                  Number of port to expose on the pod's IP address.
                                  This must be a valid port number, 0 < x < 65536.
                                format: int32
                                type: integer
                              hostIP:
                                description: What host IP to bind the external port
                                  to.
                                type: string
                              hostPort:
                                description: |-
                                  Number of port to expose on the host.
                                  If specified, this must be a valid port number, 0 < x < 65536.
                                  If HostNetwork is specified, this must match ContainerPort.
                                  Most containers do not need this.
                                format: int32
                                type: integer
                              name:
                                description: |-
                                  If specified, this must be an IANA_SVC_NAME and unique within the pod. Each
                                  named port in a pod must have a unique name. Name for the port that can be
                                  referred to by services.
                                type: string
                              protocol:
                                default: TCP
                                description: |-
                                  Protocol for port. Must be UDP, TCP, or SCTP.
                                  Defaults to "TCP".
                                type: string
                            required:
                              - containerPort
                            type: object
                          type: array
                        privateKey:
                          description: PrivateKey is a private key used for a certificate/private-key
                            pair
                          type: string
                        proxyServerIngress:
                          description: ProxyServerIngress is the authorization proxy
                            server ingress configuration
                          items:
                            description: ProxyServerIngress is the authorization ingress
                              configuration struct
                            properties:
                              annotations:
                                additionalProperties:
                                  type: string
                                description: Annotations is an unstructured key value
                                  map that stores additional annotations for the ingress
                                type: object
                              hosts:
                                description: Hosts is the hosts rules for the ingress
                                items:
                                  type: string
                                type: array
                              ingressClassName:
                                description: IngressClassName is the ingressClassName
                                type: string
                            type: object
                          type: array
                        proxyService:
                          description: ProxyService is the image tag for the Container
                          type: string
                        proxyServiceReplicas:
                          description: ProxyServiceReplicas is the number of replicas
                            for the proxy service deployment
                          type: integer
                        redis:
                          description: Redis is the image tag for the Container
                          type: string
                        redisCommander:
                          description: RedisCommander is the name of the redis deployment
                          type: string
                        redisName:
                          description: RedisName is the name of the redis statefulset
                          type: string
                        redisPassword:
                          description: RedisPassword is the password for the redis
                            instance
                          type: string
                        redisReplicas:
                          description: RedisReplicas is the number of replicas for
                            the redis deployment
                          type: integer
                        redisSecretProviderClass:
                          description: |-
                            RedisSecretProviderClass is the SecretProviderClass Object details for redis
                            Applicable from CSM v1.15 onwards
                          items:
                            description: RedisSecretProviderClass is the redis secret
                              configuration for CSM Authorization
                            properties:
                              conjur:
                                description: Conjur is the secret configuration with
                                  path to retrieve the Redis credentials from Conjur
                                properties:
                                  passwordPath:
                                    description: PasswordPath is the path to the password
                                      in the secret
                                    type: string
                                  usernamePath:
                                    description: UsernamePath is the path to the username
                                      in the secret
                                    type: string
                                type: object
                              redisPasswordKey:
                                description: RedisPasswordKey is the key in the secret
                                  that holds the Redis password
                                type: string
                              redisSecretName:
                                description: RedisSecretName is the name of the Kubernetes
                                  secret created by the CSI driver
                                type: string
                              redisUsernameKey:
                                description: RedisUsernameKey is the key in the secret
                                  that holds the Redis username
                                type: string
                              secretProviderClassName:
                                description: SecretProviderClassName is the name of
                                  the SecretProviderClass that holds the Redis secretObject
                                type: string
                            type: object
                          maxItems: 1
                          type: array
                        redisUsername:
                          description: RedisUsername is the username for the redis
                            instance
                          type: string
                        roleService:
                          description: RoleService is the image tag for the Container
                          type: string
                        roleServiceReplicas:
                          description: RoleServiceReplicas is the number of replicas
                            for the role service deployment
                          type: integer
                        secretProviderClasses:
                          description: |-
                            SecretProviderClasses is a collection of secret provider classes for retrieving secrets from external providers for storage system credentials
                            Applicable from CSM v1.15 onwards
                            Only one of SecretProviderClasses or Secrets must be specified (mutually exclusive)
                          properties:
                            conjur:
                              description: Conjur is the list SecretProviderClass
                                names provided by Conjur
                              items:
                                properties:
                                  name:
                                    description: Name is the name of the Conjur SecretProviderClass
                                    type: string
                                  paths:
                                    description: Paths is the list of paths to the
                                      secrets in Conjur
                                    items:
                                      properties:
                                        passwordPath:
                                          description: PasswordPath is the path to
                                            the password in the secret
                                          type: string
                                        usernamePath:
                                          description: UsernamePath is the path to
                                            the username in the secret
                                          type: string
                                      type: object
                                    type: array
                                type: object
                              type: array
                            vault:
                              description: Vault is the list SecretProviderClass names
                                provided by Vault
                              items:
                                type: string
                              type: array
                          type: object
                        secrets:
                          description: |-
                            Secrets is a collection of kubernetes secrets for storage system credentials
                            Applicable from CSM v1.15 onwards
                            Only one of SecretProviderClasses or Secrets must be specified (mutually exclusive)
                          items:
                            type: string
                          type: array
                        sentinel:
                          description: Sentinel is the name of the sentinel statefulSet
                          type: string
                        skipCertificateValidation:
                          description: skipCertificateValidation is the flag to skip
                            certificate validation
                          type: boolean
                        storageService:
                          description: StorageService is the image tag for the Container
                          type: string
                        storageServiceReplicas:
                          description: StorageServiceReplicas is the number of replicas
                            for storage service deployment
                          type: integer
                        tenantService:
                          description: TenantService is the image tag for the Container
                          type: string
                        tenantServiceReplicas:
                          description: TenantServiceReplicas is the number of replicas
                            for the tenant service deployment
                          type: integer
                        tolerations:
                          description: Tolerations is the list of tolerations for
                            the driver pods
                          items:
                            description: |-
                              The pod this Toleration is attached to tolerates any taint that matches
                              the triple <key,value,effect> using the matching operator <operator>.
                            properties:
                              effect:
                                description: |-
                                  Effect indicates the taint effect to match. Empty means match all taint effects.
                                  When specified, allowed values are NoSchedule, PreferNoSchedule and NoExecute.
                                type: string
                              key:
                                description: |-
                                  Key is the taint key that the toleration applies to. Empty means match all taint keys.
                                  If the key is empty, operator must be Exists; this combination means to match all values and all keys.
                                type: string
                              operator:
                                description: |-
                                  Operator represents a key's relationship to the value.
                                  Valid operators are Exists, Equal, Lt, and Gt. Defaults to Equal.
                                  Exists is equivalent to wildcard for value, so that a pod can
                                  tolerate all taints of a particular category.
                                  Lt and Gt perform numeric comparisons (requires feature gate TaintTolerationComparisonOperators).
                                type: string
                              tolerationSeconds:
                                description: |-
                                  TolerationSeconds represents the period of time the toleration (which must be
                                  of effect NoExecute, otherwise this field is ignored) tolerates the taint. By default,
                                  it is not set, which means tolerate the taint forever (do not evict). Zero and
                                  negative values will be treated as 0 (evict immediately) by the system.
                                format: int64
                                type: integer
                              value:
                                description: |-
                                  Value is the taint value the toleration matches to.
                                  If the operator is Exists, the value should be empty, otherwise just a regular string.
                                type: string
                            type: object
                          type: array
                        vaultConfigurations:
                          description: |-
                            Vaults are the vault configurations
                            Applicable till CSM v1.14
                          items:
                            description: Vault is the configuration for a vault instance
                              struct
                            properties:
                              address:
                                description: Address is the address for this vault
                                type: string
                              certificateAuthority:
                                description: CertificateAuthority is the base64-encoded
                                  certificate authority for validaitng the vault certificate
                                type: string
                              clientCertificate:
                                description: ClientCertificate is the base64-encoded
                                  certificate for connecting to vault
                                type: string
                              clientKey:
                                description: ClientKey validates is the base64-encoded
                                  certificate key for connecting to vault
                                type: string
                              identifier:
                                description: Identifier is the identifier for this
                                  vault
                                type: string
                              role:
                                description: Role is the role for this vault
                                type: string
                              skipCertificateValidation:
                                description: SkipCertificateValidation validates the
                                  vault server certificate or not
                                type: boolean
                            type: object
                          type: array
                        volumeMounts:
                          description: VolumeMounts is the list of volume mounts for
                            the container
                          items:
                            description: VolumeMount describes a mounting of a Volume
                              within a container.
                            properties:
                              mountPath:
                                description: |-
                                  Path within the container at which the volume should be mounted.  Must
                                  not contain ':'.
                                type: string
                              mountPropagation:
                                description: |-
                                  mountPropagation determines how mounts are propagated from the host
                                  to container and the other way around.
                                  When not set, MountPropagationNone is used.
                                  This field is beta in 1.10.
                                  When RecursiveReadOnly is set to IfPossible or to Enabled, MountPropagation must be None or unspecified
                                  (which defaults to None).
                                type: string
                              name:
                                description: This must match the Name of a Volume.
                                type: string
                              readOnly:
                                description: |-
                                  Mounted read-only if true, read-write otherwise (false or unspecified).
                                  Defaults to false.
                                type: boolean
                              recursiveReadOnly:
                                description: |-
                                  RecursiveReadOnly specifies whether read-only mounts should be handled
                                  recursively.

                                  If ReadOnly is false, this field has no meaning and must be unspecified.

                                  If ReadOnly is true, and this field is set to Disabled, the mount is not made
                                  recursively read-only.  If this field is set to IfPossible, the mount is made
                                  recursively read-only, if it is supported by the container runtime.  If this
                                  field is set to Enabled, the mount is made recursively read-only if it is
                                  supported by the container runtime, otherwise the pod will not be started and
                                  an error will be generated to indicate the reason.

                                  If this field is set to IfPossible or Enabled, MountPropagation must be set to
                                  None (or be unspecified, which defaults to None).

                                  If this field is not specified, it is treated as an equivalent of Disabled.
                                type: string
                              subPath:
                                description: |-
                                  Path within the volume from which the container's volume should be mounted.
                                  Defaults to "" (volume's root).
                                type: string
                              subPathExpr:
                                description: |-
                                  Expanded path within the volume from which the container's volume should be mounted.
                                  Behaves similarly to SubPath but environment variable references $(VAR_NAME) are expanded using the container's environment.
                                  Defaults to "" (volume's root).
                                  SubPathExpr and SubPath are mutually exclusive.
                                type: string
                            required:
                              - mountPath
                              - name
                            type: object
                          type: array
                      type: object
                    csiDriverSpec:
                      description: CSIDriverSpec is the specification for CSIDriver
                      properties:
                        fSGroupPolicy:
                          type: string
                        storageCapacity:
                          type: boolean
                      type: object
                    csiDriverType:
                      description: CSIDriverType is the CSI Driver type for Dell Technologies
                        - e.g, powermax, powerflex,...
                      type: string
                    dnsPolicy:
                      description: DNSPolicy is the dnsPolicy of the daemonset for
                        Node plugin
                      type: string
                    forceRemoveDriver:
                      description: ForceRemoveDriver is the boolean flag used to remove
                        driver deployment when CR is deleted
                      type: boolean
                    initContainers:
                      description: InitContainers is the specification for Driver
                        InitContainers
                      items:
                        description: ContainerTemplate template
                        properties:
                          args:
                            description: Args is the set of arguments for the container
                            items:
                              type: string
                            type: array
                          authorizationController:
                            description: AuthorizationController is the image tag
                              for the container
                            type: string
                          authorizationControllerReplicas:
                            description: AuthorizationControllerReplicas is the number
                              of replicas for the authorization controller deployment
                            type: integer
                          certificate:
                            description: Certificate is a certificate used for a certificate/private-key
                              pair
                            type: string
                          certificateAuthority:
                            description: CertificateAuthority is a certificate authority
                              used to validate a certificate
                            type: string
                          commander:
                            description: Commander is the image tag for the Container
                            type: string
                          configSecretProviderClass:
                            description: |-
                              ConfigSecretProviderClass is the SecretProviderClass Object details for config secret
                              Applicable from CSM v1.15 onwards
                            items:
                              description: ConfigSecretProviderClass is the config
                                secret configuration for CSM Authorization
                              properties:
                                configSecretName:
                                  description: ConfigSecretName is the name of the
                                    Kubernetes secret created by the CSI driver
                                  type: string
                                conjur:
                                  description: Conjur is the secret configuration
                                    with path to retrieve the Config secret from Conjur
                                  properties:
                                    secretPath:
                                      description: SecretPath is the path to the config
                                        secret
                                      type: string
                                  type: object
                                secretProviderClassName:
                                  description: SecretProviderClassName is the name
                                    of the SecretProviderClass that holds the config
                                    secretObject
                                  type: string
                              type: object
                            maxItems: 1
                            type: array
                          controllerReconcileInterval:
                            description: The interval which the reconcile of each
                              controller is run
                            type: string
                          enabled:
                            description: Enabled is used to indicate wether or not
                              to deploy a module
                            type: boolean
                          envs:
                            description: Envs is the set of environment variables
                              for the container
                            items:
                              description: EnvVar represents an environment variable
                                present in a Container.
                              properties:
                                name:
                                  description: |-
                                    Name of the environment variable.
                                    May consist of any printable ASCII characters except '='.
                                  type: string
                                value:
                                  description: |-
                                    Variable references $(VAR_NAME) are expanded
                                    using the previously defined environment variables in the container and
                                    any service environment variables. If a variable cannot be resolved,
                                    the reference in the input string will be unchanged. Double $$ are reduced
                                    to a single $, which allows for escaping the $(VAR_NAME) syntax: i.e.
                                    "$$(VAR_NAME)" will produce the string literal "$(VAR_NAME)".
                                    Escaped references will never be expanded, regardless of whether the variable
                                    exists or not.
                                    Defaults to "".
                                  type: string
                                valueFrom:
                                  description: Source for the environment variable's
                                    value. Cannot be used if value is not empty.
                                  properties:
                                    configMapKeyRef:
                                      description: Selects a key of a ConfigMap.
                                      properties:
                                        key:
                                          description: The key to select.
                                          type: string
                                        name:
                                          default: ""
                                          description: |-
                                            Name of the referent.
                                            This field is effectively required, but due to backwards compatibility is
                                            allowed to be empty. Instances of this type with an empty value here are
                                            almost certainly wrong.
                                            More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                          type: string
                                        optional:
                                          description: Specify whether the ConfigMap
                                            or its key must be defined
                                          type: boolean
                                      required:
                                        - key
                                      type: object
                                      x-kubernetes-map-type: atomic
                                    fieldRef:
                                      description: |-
                                        Selects a field of the pod: supports metadata.name, metadata.namespace, `metadata.labels['<KEY>']`, `metadata.annotations['<KEY>']`,
                                        spec.nodeName, spec.serviceAccountName, status.hostIP, status.podIP, status.podIPs.
                                      properties:
                                        apiVersion:
                                          description: Version of the schema the FieldPath
                                            is written in terms of, defaults to "v1".
                                          type: string
                                        fieldPath:
                                          description: Path of the field to select
                                            in the specified API version.
                                          type: string
                                      required:
                                        - fieldPath
                                      type: object
                                      x-kubernetes-map-type: atomic
                                    fileKeyRef:
                                      description: |-
                                        FileKeyRef selects a key of the env file.
                                        Requires the EnvFiles feature gate to be enabled.
                                      properties:
                                        key:
                                          description: |-
                                            The key within the env file. An invalid key will prevent the pod from starting.
                                            The keys defined within a source may consist of any printable ASCII characters except '='.
                                            During Alpha stage of the EnvFiles feature gate, the key size is limited to 128 characters.
                                          type: string
                                        optional:
                                          default: false
                                          description: |-
                                            Specify whether the file or its key must be defined. If the file or key
                                            does not exist, then the env var is not published.
                                            If optional is set to true and the specified key does not exist,
                                            the environment variable will not be set in the Pod's containers.

                                            If optional is set to false and the specified key does not exist,
                                            an error will be returned during Pod creation.
                                          type: boolean
                                        path:
                                          description: |-
                                            The path within the volume from which to select the file.
                                            Must be relative and may not contain the '..' path or start with '..'.
                                          type: string
                                        volumeName:
                                          description: The name of the volume mount
                                            containing the env file.
                                          type: string
                                      required:
                                        - key
                                        - path
                                        - volumeName
                                      type: object
                                      x-kubernetes-map-type: atomic
                                    resourceFieldRef:
                                      description: |-
                                        Selects a resource of the container: only resources limits and requests
                                        (limits.cpu, limits.memory, limits.ephemeral-storage, requests.cpu, requests.memory and requests.ephemeral-storage) are currently supported.
                                      properties:
                                        containerName:
                                          description: "Container name: required for
                                            volumes, optional for env vars"
                                          type: string
                                        divisor:
                                          anyOf:
                                            - type: integer
                                            - type: string
                                          description: Specifies the output format
                                            of the exposed resources, defaults to
                                            "1"
                                          pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                          x-kubernetes-int-or-string: true
                                        resource:
                                          description: "Required: resource to select"
                                          type: string
                                      required:
                                        - resource
                                      type: object
                                      x-kubernetes-map-type: atomic
                                    secretKeyRef:
                                      description: Selects a key of a secret in the
                                        pod's namespace
                                      properties:
                                        key:
                                          description: The key of the secret to select
                                            from.  Must be a valid secret key.
                                          type: string
                                        name:
                                          default: ""
                                          description: |-
                                            Name of the referent.
                                            This field is effectively required, but due to backwards compatibility is
                                            allowed to be empty. Instances of this type with an empty value here are
                                            almost certainly wrong.
                                            More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                          type: string
                                        optional:
                                          description: Specify whether the Secret
                                            or its key must be defined
                                          type: boolean
                                      required:
                                        - key
                                      type: object
                                      x-kubernetes-map-type: atomic
                                  type: object
                              required:
                                - name
                              type: object
                            maxItems: 30
                            type: array
                          gateway:
                            description: Gateway is the gateway configuration for
                              the authorization proxy-server (v2.5.0+)
                            properties:
                              annotations:
                                additionalProperties:
                                  type: string
                                description: Annotations is an unstructured key value
                                  map that stores additional annotations for the proxy-server
                                  HTTPRoute
                                type: object
                              gatewayClassName:
                                description: GatewayClassName is the name of the GatewayClass
                                  for the proxy-server Gateway resource
                                type: string
                              hosts:
                                description: Hosts is the additional hostnames for
                                  the proxy-server HTTPRoute
                                items:
                                  type: string
                                type: array
                            type: object
                          hostname:
                            description: Hostname is the authorization proxy server
                              hostname
                            type: string
                          image:
                            description: Image is the image tag for the Container
                            type: string
                          imagePullPolicy:
                            description: ImagePullPolicy is the image pull policy
                              for the image
                            type: string
                          leaderElection:
                            description: LeaderElection is boolean flag to enable
                              leader election
                            type: boolean
                          name:
                            description: Name is the name of Container
                            type: string
                          nodeSelector:
                            additionalProperties:
                              type: string
                            description: |-
                              NodeSelector is a selector which must be true for the pod to fit on a node.
                              Selector which must match a node's labels for the pod to be scheduled on that node.
                            type: object
                          opa:
                            description: Opa is the image tag for the Container
                            type: string
                          opaKubeMgmt:
                            description: OpaKubeMgmt is the image tag for the Container
                            type: string
                          openTelemetryCollectorAddress:
                            description: OpenTelemetryCollectorAddress is the address
                              of the OTLP receiving endpoint using gRPC
                            type: string
                          ports:
                            description: Ports is the list of ports for the container
                            items:
                              description: ContainerPort represents a network port
                                in a single container.
                              properties:
                                containerPort:
                                  description: |-
                                    Number of port to expose on the pod's IP address.
                                    This must be a valid port number, 0 < x < 65536.
                                  format: int32
                                  type: integer
                                hostIP:
                                  description: What host IP to bind the external port
                                    to.
                                  type: string
                                hostPort:
                                  description: |-
                                    Number of port to expose on the host.
                                    If specified, this must be a valid port number, 0 < x < 65536.
                                    If HostNetwork is specified, this must match ContainerPort.
                                    Most containers do not need this.
                                  format: int32
                                  type: integer
                                name:
                                  description: |-
                                    If specified, this must be an IANA_SVC_NAME and unique within the pod. Each
                                    named port in a pod must have a unique name. Name for the port that can be
                                    referred to by services.
                                  type: string
                                protocol:
                                  default: TCP
                                  description: |-
                                    Protocol for port. Must be UDP, TCP, or SCTP.
                                    Defaults to "TCP".
                                  type: string
                              required:
                                - containerPort
                              type: object
                            type: array
                          privateKey:
                            description: PrivateKey is a private key used for a certificate/private-key
                              pair
                            type: string
                          proxyServerIngress:
                            description: ProxyServerIngress is the authorization proxy
                              server ingress configuration
                            items:
                              description: ProxyServerIngress is the authorization
                                ingress configuration struct
                              properties:
                                annotations:
                                  additionalProperties:
                                    type: string
                                  description: Annotations is an unstructured key
                                    value map that stores additional annotations for
                                    the ingress
                                  type: object
                                hosts:
                                  description: Hosts is the hosts rules for the ingress
                                  items:
                                    type: string
                                  type: array
                                ingressClassName:
                                  description: IngressClassName is the ingressClassName
                                  type: string
                              type: object
                            type: array
                          proxyService:
                            description: ProxyService is the image tag for the Container
                            type: string
                          proxyServiceReplicas:
                            description: ProxyServiceReplicas is the number of replicas
                              for the proxy service deployment
                            type: integer
                          redis:
                            description: Redis is the image tag for the Container
                            type: string
                          redisCommander:
                            description: RedisCommander is the name of the redis deployment
                            type: string
                          redisName:
                            description: RedisName is the name of the redis statefulset
                            type: string
                          redisPassword:
                            description: RedisPassword is the password for the redis
                              instance
                            type: string
                          redisReplicas:
                            description: RedisReplicas is the number of replicas for
                              the redis deployment
                            type: integer
                          redisSecretProviderClass:
                            description: |-
                              RedisSecretProviderClass is the SecretProviderClass Object details for redis
                              Applicable from CSM v1.15 onwards
                            items:
                              description: RedisSecretProviderClass is the redis secret
                                configuration for CSM Authorization
                              properties:
                                conjur:
                                  description: Conjur is the secret configuration
                                    with path to retrieve the Redis credentials from
                                    Conjur
                                  properties:
                                    passwordPath:
                                      description: PasswordPath is the path to the
                                        password in the secret
                                      type: string
                                    usernamePath:
                                      description: UsernamePath is the path to the
                                        username in the secret
                                      type: string
                                  type: object
                                redisPasswordKey:
                                  description: RedisPasswordKey is the key in the
                                    secret that holds the Redis password
                                  type: string
                                redisSecretName:
                                  description: RedisSecretName is the name of the
                                    Kubernetes secret created by the CSI driver
                                  type: string
                                redisUsernameKey:
                                  description: RedisUsernameKey is the key in the
                                    secret that holds the Redis username
                                  type: string
                                secretProviderClassName:
                                  description: SecretProviderClassName is the name
                                    of the SecretProviderClass that holds the Redis
                                    secretObject
                                  type: string
                              type: object
                            maxItems: 1
                            type: array
                          redisUsername:
                            description: RedisUsername is the username for the redis
                              instance
                            type: string
                          roleService:
                            description: RoleService is the image tag for the Container
                            type: string
                          roleServiceReplicas:
                            description: RoleServiceReplicas is the number of replicas
                              for the role service deployment
                            type: integer
                          secretProviderClasses:
                            description: |-
                              SecretProviderClasses is a collection of secret provider classes for retrieving secrets from external providers for storage system credentials
                              Applicable from CSM v1.15 onwards
                              Only one of SecretProviderClasses or Secrets must be specified (mutually exclusive)
                            properties:
                              conjur:
                                description: Conjur is the list SecretProviderClass
                                  names provided by Conjur
                                items:
                                  properties:
                                    name:
                                      description: Name is the name of the Conjur
                                        SecretProviderClass
                                      type: string
                                    paths:
                                      description: Paths is the list of paths to the
                                        secrets in Conjur
                                      items:
                                        properties:
                                          passwordPath:
                                            description: PasswordPath is the path
                                              to the password in the secret
                                            type: string
                                          usernamePath:
                                            description: UsernamePath is the path
                                              to the username in the secret
                                            type: string
                                        type: object
                                      type: array
                                  type: object
                                type: array
                              vault:
                                description: Vault is the list SecretProviderClass
                                  names provided by Vault
                                items:
                                  type: string
                                type: array
                            type: object
                          secrets:
                            description: |-
                              Secrets is a collection of kubernetes secrets for storage system credentials
                              Applicable from CSM v1.15 onwards
                              Only one of SecretProviderClasses or Secrets must be specified (mutually exclusive)
                            items:
                              type: string
                            type: array
                          sentinel:
                            description: Sentinel is the name of the sentinel statefulSet
                            type: string
                          skipCertificateValidation:
                            description: skipCertificateValidation is the flag to
                              skip certificate validation
                            type: boolean
                          storageService:
                            description: StorageService is the image tag for the Container
                            type: string
                          storageServiceReplicas:
                            description: StorageServiceReplicas is the number of replicas
                              for storage service deployment
                            type: integer
                          tenantService:
                            description: TenantService is the image tag for the Container
                            type: string
                          tenantServiceReplicas:
                            description: TenantServiceReplicas is the number of replicas
                              for the tenant service deployment
                            type: integer
                          tolerations:
                            description: Tolerations is the list of tolerations for
                              the driver pods
                            items:
                              description: |-
                                The pod this Toleration is attached to tolerates any taint that matches
                                the triple <key,value,effect> using the matching operator <operator>.
                              properties:
                                effect:
                                  description: |-
                                    Effect indicates the taint effect to match. Empty means match all taint effects.
                                    When specified, allowed values are NoSchedule, PreferNoSchedule and NoExecute.
                                  type: string
                                key:
                                  description: |-
                                    Key is the taint key that the toleration applies to. Empty means match all taint keys.
                                    If the key is empty, operator must be Exists; this combination means to match all values and all keys.
                                  type: string
                                operator:
                                  description: |-
                                    Operator represents a key's relationship to the value.
                                    Valid operators are Exists, Equal, Lt, and Gt. Defaults to Equal.
                                    Exists is equivalent to wildcard for value, so that a pod can
                                    tolerate all taints of a particular category.
                                    Lt and Gt perform numeric comparisons (requires feature gate TaintTolerationComparisonOperators).
                                  type: string
                                tolerationSeconds:
                                  description: |-
                                    TolerationSeconds represents the period of time the toleration (which must be
                                    of effect NoExecute, otherwise this field is ignored) tolerates the taint. By default,
                                    it is not set, which means tolerate the taint forever (do not evict). Zero and
                                    negative values will be treated as 0 (evict immediately) by the system.
                                  format: int64
                                  type: integer
                                value:
                                  description: |-
                                    Value is the taint value the toleration matches to.
                                    If the operator is Exists, the value should be empty, otherwise just a regular string.
                                  type: string
                              type: object
                            type: array
                          vaultConfigurations:
                            description: |-
                              Vaults are the vault configurations
                              Applicable till CSM v1.14
                            items:
                              description: Vault is the configuration for a vault
                                instance struct
                              properties:
                                address:
                                  description: Address is the address for this vault
                                  type: string
                                certificateAuthority:
                                  description: CertificateAuthority is the base64-encoded
                                    certificate authority for validaitng the vault
                                    certificate
                                  type: string
                                clientCertificate:
                                  description: ClientCertificate is the base64-encoded
                                    certificate for connecting to vault
                                  type: string
                                clientKey:
                                  description: ClientKey validates is the base64-encoded
                                    certificate key for connecting to vault
                                  type: string
                                identifier:
                                  description: Identifier is the identifier for this
                                    vault
                                  type: string
                                role:
                                  description: Role is the role for this vault
                                  type: string
                                skipCertificateValidation:
                                  description: SkipCertificateValidation validates
                                    the vault server certificate or not
                                  type: boolean
                              type: object
                            type: array
                          volumeMounts:
                            description: VolumeMounts is the list of volume mounts
                              for the container
                            items:
                              description: VolumeMount describes a mounting of a Volume
                                within a container.
                              properties:
                                mountPath:
                                  description: |-
                                    Path within the container at which the volume should be mounted.  Must
                                    not contain ':'.
                                  type: string
                                mountPropagation:
                                  description: |-
                                    mountPropagation determines how mounts are propagated from the host
                                    to container and the other way around.
                                    When not set, MountPropagationNone is used.
                                    This field is beta in 1.10.
                                    When RecursiveReadOnly is set to IfPossible or to Enabled, MountPropagation must be None or unspecified
                                    (which defaults to None).
                                  type: string
                                name:
                                  description: This must match the Name of a Volume.
                                  type: string
                                readOnly:
                                  description: |-
                                    Mounted read-only if true, read-write otherwise (false or unspecified).
                                    Defaults to false.
                                  type: boolean
                                recursiveReadOnly:
                                  description: |-
                                    RecursiveReadOnly specifies whether read-only mounts should be handled
                                    recursively.

                                    If ReadOnly is false, this field has no meaning and must be unspecified.

                                    If ReadOnly is true, and this field is set to Disabled, the mount is not made
                                    recursively read-only.  If this field is set to IfPossible, the mount is made
                                    recursively read-only, if it is supported by the container runtime.  If this
                                    field is set to Enabled, the mount is made recursively read-only if it is
                                    supported by the container runtime, otherwise the pod will not be started and
                                    an error will be generated to indicate the reason.

                                    If this field is set to IfPossible or Enabled, MountPropagation must be set to
                                    None (or be unspecified, which defaults to None).

                                    If this field is not specified, it is treated as an equivalent of Disabled.
                                  type: string
                                subPath:
                                  description: |-
                                    Path within the volume from which the container's volume should be mounted.
                                    Defaults to "" (volume's root).
                                  type: string
                                subPathExpr:
                                  description: |-
                                    Expanded path within the volume from which the container's volume should be mounted.
                                    Behaves similarly to SubPath but environment variable references $(VAR_NAME) are expanded using the container's environment.
                                    Defaults to "" (volume's root).
                                    SubPathExpr and SubPath are mutually exclusive.
                                  type: string
                              required:
                                - mountPath
                                - name
                              type: object
                            type: array
                        type: object
                      maxItems: 20
                      type: array
                    metrics:
                      description: Metrics is the configuration for the driver metrics
                        endpoint and monitoring
                      properties:
                        array:
                          description: Array configures array-facing metrics collection
                            controls
                          properties:
                            circuitBreaker:
                              description: CircuitBreaker configures metrics collection
                                circuit breaker controls
                              properties:
                                resetTimeout:
                                  description: ResetTimeout is how long to wait before
                                    retrying after opening the circuit
                                  type: string
                                  x-kubernetes-validations:
                                    - message: resetTimeout must be a non-empty string
                                      rule: self == '' || size(self) > 0
                                threshold:
                                  description: Threshold opens the circuit after this
                                    many consecutive failures
                                  format: int32
                                  minimum: 1
                                  type: integer
                              type: object
                            rateLimit:
                              description: RateLimit sets the max allowed array requests
                                per collection window
                              format: int32
                              minimum: 1
                              type: integer
                            timeout:
                              description: Timeout sets the timeout for metrics array
                                requests
                              type: string
                              x-kubernetes-validations:
                                - message: timeout must be a non-empty string
                                  rule: self == '' || size(self) > 0
                          type: object
                        collection:
                          description: Collection configures driver metrics collection
                            cadence and cache behavior
                          properties:
                            cacheTTL:
                              description: CacheTTL is how long collected metric responses
                                are cached
                              type: string
                              x-kubernetes-validations:
                                - message: cacheTTL must be a non-empty string
                                  rule: self == '' || size(self) > 0
                            interval:
                              description: Interval is how frequently metrics are
                                collected from the array
                              type: string
                              x-kubernetes-validations:
                                - message: interval must be a non-empty string
                                  rule: self == '' || size(self) > 0
                          type: object
                        enabled:
                          description: Enabled enables the shared Prometheus metrics
                            HTTP endpoint on the controller
                          type: boolean
                        gatewayMonitoring:
                          description: GatewayMonitoring configures PowerFlex Gateway
                            health monitoring
                          properties:
                            enabled:
                              description: Enabled enables Gateway health monitoring
                                on the controller
                              type: boolean
                            leaderElectionEnabled:
                              description: |-
                                LeaderElectionEnabled enables leader election so only one controller actively monitors gateways
                                When nil, defaults to true
                              type: boolean
                            pollInterval:
                              description: PollInterval is how frequently the leader
                                controller probes each gateway endpoint
                              type: string
                              x-kubernetes-validations:
                                - message: pollInterval must be a non-empty string
                                  rule: self == '' || size(self) > 0
                          type: object
                        leaderElection:
                          description: |-
                            LeaderElection configures leader election for array-level metrics collection
                            Only the leader controller will collect array-level metrics to avoid duplicate API calls
                          properties:
                            enabled:
                              description: |-
                                Enabled enables leader election for array-level metrics collection
                                Only the leader controller will collect array-level metrics to avoid duplicate API calls
                                When nil, defaults to false
                              type: boolean
                            leaseDuration:
                              description: |-
                                LeaseDuration is the duration that non-leader candidates will wait to acquire the lease
                                Default value: 60s
                              type: string
                            renewDeadline:
                              description: |-
                                RenewDeadline is the duration that the acting leader will retry refreshing leadership before giving up
                                Default value: 40s
                              type: string
                            retryPeriod:
                              description: |-
                                RetryPeriod is the duration the LeaderElector clients should wait between tries of actions
                                Default value: 5s
                              type: string
                          type: object
                        podMonitor:
                          description: PodMonitor configures optional Prometheus Operator
                            PodMonitor creation for node pods
                          properties:
                            enabled:
                              description: |-
                                Enabled creates a PodMonitor CR for Prometheus Operator integration targeting node pods
                                Requires the Prometheus Operator CRDs to be installed in the cluster
                              type: boolean
                            insecureSkipVerify:
                              description: |-
                                InsecureSkipVerify skips TLS certificate verification when Prometheus scrapes the node metrics endpoint
                                Only applies when tlsCertSecret is set
                              type: boolean
                            interval:
                              description: Interval is the Prometheus scrape interval
                                for the node metrics endpoint
                              type: string
                              x-kubernetes-validations:
                                - message: interval must be a non-empty string
                                  rule: self == '' || size(self) > 0
                            scrapeTimeout:
                              description: |-
                                ScrapeTimeout is the Prometheus scrape timeout for the node metrics endpoint
                                Default value: "" (unset; Prometheus uses its global scrape_timeout)
                              type: string
                              x-kubernetes-validations:
                                - message: scrapeTimeout must be a non-empty string
                                  rule: self == '' || size(self) > 0
                          type: object
                        port:
                          description: Port is the port on which the metrics endpoint
                            is exposed
                          format: int32
                          type: integer
                        prometheusRule:
                          description: PrometheusRule configures optional Prometheus
                            Operator alert rule creation for supported drivers
                          properties:
                            apiErrorRateThreshold:
                              default: 10
                              description: |-
                                APIErrorRateThreshold is the whole-percent API error threshold that triggers an alert for drivers exposing request success and failure metrics.
                                PowerScale uses this field.
                              format: int32
                              type: integer
                            apiErrorRateWindow:
                              default: 5m
                              description: |-
                                APIErrorRateWindow is the lookback window used when evaluating the API error ratio alert expression.
                                PowerScale uses this field.
                              type: string
                            applianceCriticalThreshold:
                              default: 90
                              description: |-
                                ApplianceCriticalThreshold is the whole-percent appliance utilization threshold that triggers a critical alert for drivers exposing appliance metrics.
                                PowerStore uses this field.
                              format: int32
                              type: integer
                            applianceWarningThreshold:
                              default: 80
                              description: |-
                                ApplianceWarningThreshold is the whole-percent appliance utilization threshold that triggers a warning alert for drivers exposing appliance metrics.
                                PowerStore uses this field.
                              format: int32
                              type: integer
                            connectivitySuccessRatio:
                              default: 90
                              description: |-
                                ConnectivitySuccessRatio is the minimum percentage of successful array connectivity checks
                                below which the RES-02 CSMResiliencyConnectivityLost alert fires.
                                This field is only read when set under spec.modules[name=resiliency].metrics.prometheusRule.
                                Setting it under spec.driver.metrics.prometheusRule has no effect on resiliency alerts.
                              format: int32
                              maximum: 100
                              minimum: 1
                              type: integer
                            cpuWarningThreshold:
                              default: 80
                              description: |-
                                CPUWarningThreshold is the whole-percent CPU utilization threshold that triggers a warning alert for driver pods.
                                PowerScale uses this field.
                              format: int32
                              type: integer
                            driverCrashLoopRestartThreshold:
                              default: 3
                              description: |-
                                DriverCrashLoopRestartThreshold is the restart count threshold used to detect crash looping within the configured window.
                                PowerScale uses this field.
                              format: int32
                              type: integer
                            driverCrashLoopWindow:
                              default: 15m
                              description: |-
                                DriverCrashLoopWindow is the lookback window used when evaluating crash loop restart deltas.
                                PowerScale uses this field.
                              type: string
                            enabled:
                              default: true
                              description: Enabled creates a PrometheusRule CR for
                                Prometheus Operator integration.
                              type: boolean
                            memoryWarningThreshold:
                              default: 2147483648
                              description: |-
                                MemoryWarningThreshold is the driver memory usage threshold in bytes that triggers a warning alert.
                                PowerScale uses this field.
                              format: int64
                              type: integer
                            nfsV3LatencyWarningSeconds:
                              default: 10
                              description: |-
                                NFSv3LatencyWarningSeconds is the NFSv3 p99 latency threshold in seconds that triggers a warning alert.
                                PowerScale uses this field.
                              format: int32
                              type: integer
                            nfsV4LatencyWarningSeconds:
                              default: 10
                              description: |-
                                NFSv4LatencyWarningSeconds is the NFSv4 p99 latency threshold in seconds that triggers a warning alert.
                                PowerScale uses this field.
                              format: int32
                              type: integer
                            nodepoolCriticalThreshold:
                              default: 90
                              description: |-
                                NodepoolCriticalThreshold is the whole-percent node pool utilization threshold that triggers a critical alert for drivers exposing node pool metrics.
                                PowerScale uses this field.
                              format: int32
                              type: integer
                            nodepoolWarningThreshold:
                              default: 80
                              description: |-
                                NodepoolWarningThreshold is the whole-percent node pool utilization threshold that triggers a warning alert for drivers exposing node pool metrics.
                                PowerScale uses this field.
                              format: int32
                              type: integer
                            quotaCriticalThreshold:
                              default: 90
                              description: |-
                                QuotaCriticalThreshold is the whole-percent quota utilization threshold that triggers a critical alert for drivers exposing quota metrics.
                                PowerScale uses this field.
                              format: int32
                              type: integer
                            quotaWarningThreshold:
                              default: 80
                              description: |-
                                QuotaWarningThreshold is the whole-percent quota utilization threshold that triggers a warning alert for drivers exposing quota metrics.
                                PowerScale uses this field.
                              format: int32
                              type: integer
                            srpSnapshotCapacityWarning:
                              default: 80
                              description: |-
                                SRPSnapshotCapacityWarning is the whole-percent SRP snapshot capacity threshold that triggers
                                a warning alert. PowerMax uses this field (PM-15). SRP-level monitoring is the permanent
                                solution (per-SG snapshot metric is not required per product management decision).
                              format: int32
                              type: integer
                            storageGroupCapacityCritical:
                              default: 90
                              description: |-
                                StorageGroupCapacityCritical is the whole-percent Storage Group utilization threshold that
                                triggers a critical alert. PowerMax uses this field (PM-14).
                              format: int32
                              type: integer
                            storageGroupCapacityWarning:
                              default: 80
                              description: |-
                                StorageGroupCapacityWarning is the whole-percent Storage Group utilization threshold that
                                triggers a warning alert. PowerMax uses this field (PM-13).
                              format: int32
                              type: integer
                            volumeOperationFailureThreshold:
                              default: 3
                              description: |-
                                VolumeOperationFailureThreshold is the cumulative failure count threshold for volume operations
                                (CreateVolume, DeleteVolume, ControllerPublishVolume, ControllerUnpublishVolume,
                                NodeStageVolume, NodeUnstageVolume, NodePublishVolume, NodeUnpublishVolume) that triggers a
                                warning alert. PowerMax uses this field (PM-01 to PM-08). PowerStore uses this field.
                              format: int32
                              type: integer
                          type: object
                        serviceMonitor:
                          description: ServiceMonitor configures optional Prometheus
                            Operator ServiceMonitor creation
                          properties:
                            enabled:
                              description: |-
                                Enabled creates a ServiceMonitor CR for Prometheus Operator integration
                                Requires the Prometheus Operator CRDs to be installed in the cluster
                              type: boolean
                            insecureSkipVerify:
                              description: |-
                                InsecureSkipVerify skips TLS certificate verification when Prometheus scrapes the metrics endpoint
                                Only applies when tlsCertSecret is set
                              type: boolean
                            interval:
                              description: Interval is the Prometheus scrape interval
                                for the metrics endpoint
                              type: string
                              x-kubernetes-validations:
                                - message: interval must be a non-empty string
                                  rule: self == '' || size(self) > 0
                            scrapeTimeout:
                              description: |-
                                ScrapeTimeout is the Prometheus scrape timeout for the metrics endpoint
                                Default value: "" (unset; Prometheus uses its global scrape_timeout)
                              type: string
                              x-kubernetes-validations:
                                - message: scrapeTimeout must be a non-empty string
                                  rule: self == '' || size(self) > 0
                          type: object
                        tlsCertSecret:
                          description: TLSCertSecret is the name of a Kubernetes TLS
                            Secret used to serve the metrics endpoint over HTTPS
                          type: string
                      type: object
                    node:
                      description: Node is the specification for Node plugin only
                      properties:
                        args:
                          description: Args is the set of arguments for the container
                          items:
                            type: string
                          type: array
                        authorizationController:
                          description: AuthorizationController is the image tag for
                            the container
                          type: string
                        authorizationControllerReplicas:
                          description: AuthorizationControllerReplicas is the number
                            of replicas for the authorization controller deployment
                          type: integer
                        certificate:
                          description: Certificate is a certificate used for a certificate/private-key
                            pair
                          type: string
                        certificateAuthority:
                          description: CertificateAuthority is a certificate authority
                            used to validate a certificate
                          type: string
                        commander:
                          description: Commander is the image tag for the Container
                          type: string
                        configSecretProviderClass:
                          description: |-
                            ConfigSecretProviderClass is the SecretProviderClass Object details for config secret
                            Applicable from CSM v1.15 onwards
                          items:
                            description: ConfigSecretProviderClass is the config secret
                              configuration for CSM Authorization
                            properties:
                              configSecretName:
                                description: ConfigSecretName is the name of the Kubernetes
                                  secret created by the CSI driver
                                type: string
                              conjur:
                                description: Conjur is the secret configuration with
                                  path to retrieve the Config secret from Conjur
                                properties:
                                  secretPath:
                                    description: SecretPath is the path to the config
                                      secret
                                    type: string
                                type: object
                              secretProviderClassName:
                                description: SecretProviderClassName is the name of
                                  the SecretProviderClass that holds the config secretObject
                                type: string
                            type: object
                          maxItems: 1
                          type: array
                        controllerReconcileInterval:
                          description: The interval which the reconcile of each controller
                            is run
                          type: string
                        enabled:
                          description: Enabled is used to indicate wether or not to
                            deploy a module
                          type: boolean
                        envs:
                          description: Envs is the set of environment variables for
                            the container
                          items:
                            description: EnvVar represents an environment variable
                              present in a Container.
                            properties:
                              name:
                                description: |-
                                  Name of the environment variable.
                                  May consist of any printable ASCII characters except '='.
                                type: string
                              value:
                                description: |-
                                  Variable references $(VAR_NAME) are expanded
                                  using the previously defined environment variables in the container and
                                  any service environment variables. If a variable cannot be resolved,
                                  the reference in the input string will be unchanged. Double $$ are reduced
                                  to a single $, which allows for escaping the $(VAR_NAME) syntax: i.e.
                                  "$$(VAR_NAME)" will produce the string literal "$(VAR_NAME)".
                                  Escaped references will never be expanded, regardless of whether the variable
                                  exists or not.
                                  Defaults to "".
                                type: string
                              valueFrom:
                                description: Source for the environment variable's
                                  value. Cannot be used if value is not empty.
                                properties:
                                  configMapKeyRef:
                                    description: Selects a key of a ConfigMap.
                                    properties:
                                      key:
                                        description: The key to select.
                                        type: string
                                      name:
                                        default: ""
                                        description: |-
                                          Name of the referent.
                                          This field is effectively required, but due to backwards compatibility is
                                          allowed to be empty. Instances of this type with an empty value here are
                                          almost certainly wrong.
                                          More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                        type: string
                                      optional:
                                        description: Specify whether the ConfigMap
                                          or its key must be defined
                                        type: boolean
                                    required:
                                      - key
                                    type: object
                                    x-kubernetes-map-type: atomic
                                  fieldRef:
                                    description: |-
                                      Selects a field of the pod: supports metadata.name, metadata.namespace, `metadata.labels['<KEY>']`, `metadata.annotations['<KEY>']`,
                                      spec.nodeName, spec.serviceAccountName, status.hostIP, status.podIP, status.podIPs.
                                    properties:
                                      apiVersion:
                                        description: Version of the schema the FieldPath
                                          is written in terms of, defaults to "v1".
                                        type: string
                                      fieldPath:
                                        description: Path of the field to select in
                                          the specified API version.
                                        type: string
                                    required:
                                      - fieldPath
                                    type: object
                                    x-kubernetes-map-type: atomic
                                  fileKeyRef:
                                    description: |-
                                      FileKeyRef selects a key of the env file.
                                      Requires the EnvFiles feature gate to be enabled.
                                    properties:
                                      key:
                                        description: |-
                                          The key within the env file. An invalid key will prevent the pod from starting.
                                          The keys defined within a source may consist of any printable ASCII characters except '='.
                                          During Alpha stage of the EnvFiles feature gate, the key size is limited to 128 characters.
                                        type: string
                                      optional:
                                        default: false
                                        description: |-
                                          Specify whether the file or its key must be defined. If the file or key
                                          does not exist, then the env var is not published.
                                          If optional is set to true and the specified key does not exist,
                                          the environment variable will not be set in the Pod's containers.

                                          If optional is set to false and the specified key does not exist,
                                          an error will be returned during Pod creation.
                                        type: boolean
                                      path:
                                        description: |-
                                          The path within the volume from which to select the file.
                                          Must be relative and may not contain the '..' path or start with '..'.
                                        type: string
                                      volumeName:
                                        description: The name of the volume mount
                                          containing the env file.
                                        type: string
                                    required:
                                      - key
                                      - path
                                      - volumeName
                                    type: object
                                    x-kubernetes-map-type: atomic
                                  resourceFieldRef:
                                    description: |-
                                      Selects a resource of the container: only resources limits and requests
                                      (limits.cpu, limits.memory, limits.ephemeral-storage, requests.cpu, requests.memory and requests.ephemeral-storage) are currently supported.
                                    properties:
                                      containerName:
                                        description: "Container name: required for
                                          volumes, optional for env vars"
                                        type: string
                                      divisor:
                                        anyOf:
                                          - type: integer
                                          - type: string
                                        description: Specifies the output format of
                                          the exposed resources, defaults to "1"
                                        pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                        x-kubernetes-int-or-string: true
                                      resource:
                                        description: "Required: resource to select"
                                        type: string
                                    required:
                                      - resource
                                    type: object
                                    x-kubernetes-map-type: atomic
                                  secretKeyRef:
                                    description: Selects a key of a secret in the
                                      pod's namespace
                                    properties:
                                      key:
                                        description: The key of the secret to select
                                          from.  Must be a valid secret key.
                                        type: string
                                      name:
                                        default: ""
                                        description: |-
                                          Name of the referent.
                                          This field is effectively required, but due to backwards compatibility is
                                          allowed to be empty. Instances of this type with an empty value here are
                                          almost certainly wrong.
                                          More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                        type: string
                                      optional:
                                        description: Specify whether the Secret or
                                          its key must be defined
                                        type: boolean
                                    required:
                                      - key
                                    type: object
                                    x-kubernetes-map-type: atomic
                                type: object
                            required:
                              - name
                            type: object
                          maxItems: 30
                          type: array
                        gateway:
                          description: Gateway is the gateway configuration for the
                            authorization proxy-server (v2.5.0+)
                          properties:
                            annotations:
                              additionalProperties:
                                type: string
                              description: Annotations is an unstructured key value
                                map that stores additional annotations for the proxy-server
                                HTTPRoute
                              type: object
                            gatewayClassName:
                              description: GatewayClassName is the name of the GatewayClass
                                for the proxy-server Gateway resource
                              type: string
                            hosts:
                              description: Hosts is the additional hostnames for the
                                proxy-server HTTPRoute
                              items:
                                type: string
                              type: array
                          type: object
                        hostname:
                          description: Hostname is the authorization proxy server
                            hostname
                          type: string
                        image:
                          description: Image is the image tag for the Container
                          type: string
                        imagePullPolicy:
                          description: ImagePullPolicy is the image pull policy for
                            the image
                          type: string
                        leaderElection:
                          description: LeaderElection is boolean flag to enable leader
                            election
                          type: boolean
                        name:
                          description: Name is the name of Container
                          type: string
                        nodeSelector:
                          additionalProperties:
                            type: string
                          description: |-
                            NodeSelector is a selector which must be true for the pod to fit on a node.
                            Selector which must match a node's labels for the pod to be scheduled on that node.
                          type: object
                        opa:
                          description: Opa is the image tag for the Container
                          type: string
                        opaKubeMgmt:
                          description: OpaKubeMgmt is the image tag for the Container
                          type: string
                        openTelemetryCollectorAddress:
                          description: OpenTelemetryCollectorAddress is the address
                            of the OTLP receiving endpoint using gRPC
                          type: string
                        ports:
                          description: Ports is the list of ports for the container
                          items:
                            description: ContainerPort represents a network port in
                              a single container.
                            properties:
                              containerPort:
                                description: |-
                                  Number of port to expose on the pod's IP address.
                                  This must be a valid port number, 0 < x < 65536.
                                format: int32
                                type: integer
                              hostIP:
                                description: What host IP to bind the external port
                                  to.
                                type: string
                              hostPort:
                                description: |-
                                  Number of port to expose on the host.
                                  If specified, this must be a valid port number, 0 < x < 65536.
                                  If HostNetwork is specified, this must match ContainerPort.
                                  Most containers do not need this.
                                format: int32
                                type: integer
                              name:
                                description: |-
                                  If specified, this must be an IANA_SVC_NAME and unique within the pod. Each
                                  named port in a pod must have a unique name. Name for the port that can be
                                  referred to by services.
                                type: string
                              protocol:
                                default: TCP
                                description: |-
                                  Protocol for port. Must be UDP, TCP, or SCTP.
                                  Defaults to "TCP".
                                type: string
                            required:
                              - containerPort
                            type: object
                          type: array
                        privateKey:
                          description: PrivateKey is a private key used for a certificate/private-key
                            pair
                          type: string
                        proxyServerIngress:
                          description: ProxyServerIngress is the authorization proxy
                            server ingress configuration
                          items:
                            description: ProxyServerIngress is the authorization ingress
                              configuration struct
                            properties:
                              annotations:
                                additionalProperties:
                                  type: string
                                description: Annotations is an unstructured key value
                                  map that stores additional annotations for the ingress
                                type: object
                              hosts:
                                description: Hosts is the hosts rules for the ingress
                                items:
                                  type: string
                                type: array
                              ingressClassName:
                                description: IngressClassName is the ingressClassName
                                type: string
                            type: object
                          type: array
                        proxyService:
                          description: ProxyService is the image tag for the Container
                          type: string
                        proxyServiceReplicas:
                          description: ProxyServiceReplicas is the number of replicas
                            for the proxy service deployment
                          type: integer
                        redis:
                          description: Redis is the image tag for the Container
                          type: string
                        redisCommander:
                          description: RedisCommander is the name of the redis deployment
                          type: string
                        redisName:
                          description: RedisName is the name of the redis statefulset
                          type: string
                        redisPassword:
                          description: RedisPassword is the password for the redis
                            instance
                          type: string
                        redisReplicas:
                          description: RedisReplicas is the number of replicas for
                            the redis deployment
                          type: integer
                        redisSecretProviderClass:
                          description: |-
                            RedisSecretProviderClass is the SecretProviderClass Object details for redis
                            Applicable from CSM v1.15 onwards
                          items:
                            description: RedisSecretProviderClass is the redis secret
                              configuration for CSM Authorization
                            properties:
                              conjur:
                                description: Conjur is the secret configuration with
                                  path to retrieve the Redis credentials from Conjur
                                properties:
                                  passwordPath:
                                    description: PasswordPath is the path to the password
                                      in the secret
                                    type: string
                                  usernamePath:
                                    description: UsernamePath is the path to the username
                                      in the secret
                                    type: string
                                type: object
                              redisPasswordKey:
                                description: RedisPasswordKey is the key in the secret
                                  that holds the Redis password
                                type: string
                              redisSecretName:
                                description: RedisSecretName is the name of the Kubernetes
                                  secret created by the CSI driver
                                type: string
                              redisUsernameKey:
                                description: RedisUsernameKey is the key in the secret
                                  that holds the Redis username
                                type: string
                              secretProviderClassName:
                                description: SecretProviderClassName is the name of
                                  the SecretProviderClass that holds the Redis secretObject
                                type: string
                            type: object
                          maxItems: 1
                          type: array
                        redisUsername:
                          description: RedisUsername is the username for the redis
                            instance
                          type: string
                        roleService:
                          description: RoleService is the image tag for the Container
                          type: string
                        roleServiceReplicas:
                          description: RoleServiceReplicas is the number of replicas
                            for the role service deployment
                          type: integer
                        secretProviderClasses:
                          description: |-
                            SecretProviderClasses is a collection of secret provider classes for retrieving secrets from external providers for storage system credentials
                            Applicable from CSM v1.15 onwards
                            Only one of SecretProviderClasses or Secrets must be specified (mutually exclusive)
                          properties:
                            conjur:
                              description: Conjur is the list SecretProviderClass
                                names provided by Conjur
                              items:
                                properties:
                                  name:
                                    description: Name is the name of the Conjur SecretProviderClass
                                    type: string
                                  paths:
                                    description: Paths is the list of paths to the
                                      secrets in Conjur
                                    items:
                                      properties:
                                        passwordPath:
                                          description: PasswordPath is the path to
                                            the password in the secret
                                          type: string
                                        usernamePath:
                                          description: UsernamePath is the path to
                                            the username in the secret
                                          type: string
                                      type: object
                                    type: array
                                type: object
                              type: array
                            vault:
                              description: Vault is the list SecretProviderClass names
                                provided by Vault
                              items:
                                type: string
                              type: array
                          type: object
                        secrets:
                          description: |-
                            Secrets is a collection of kubernetes secrets for storage system credentials
                            Applicable from CSM v1.15 onwards
                            Only one of SecretProviderClasses or Secrets must be specified (mutually exclusive)
                          items:
                            type: string
                          type: array
                        sentinel:
                          description: Sentinel is the name of the sentinel statefulSet
                          type: string
                        skipCertificateValidation:
                          description: skipCertificateValidation is the flag to skip
                            certificate validation
                          type: boolean
                        storageService:
                          description: StorageService is the image tag for the Container
                          type: string
                        storageServiceReplicas:
                          description: StorageServiceReplicas is the number of replicas
                            for storage service deployment
                          type: integer
                        tenantService:
                          description: TenantService is the image tag for the Container
                          type: string
                        tenantServiceReplicas:
                          description: TenantServiceReplicas is the number of replicas
                            for the tenant service deployment
                          type: integer
                        tolerations:
                          description: Tolerations is the list of tolerations for
                            the driver pods
                          items:
                            description: |-
                              The pod this Toleration is attached to tolerates any taint that matches
                              the triple <key,value,effect> using the matching operator <operator>.
                            properties:
                              effect:
                                description: |-
                                  Effect indicates the taint effect to match. Empty means match all taint effects.
                                  When specified, allowed values are NoSchedule, PreferNoSchedule and NoExecute.
                                type: string
                              key:
                                description: |-
                                  Key is the taint key that the toleration applies to. Empty means match all taint keys.
                                  If the key is empty, operator must be Exists; this combination means to match all values and all keys.
                                type: string
                              operator:
                                description: |-
                                  Operator represents a key's relationship to the value.
                                  Valid operators are Exists, Equal, Lt, and Gt. Defaults to Equal.
                                  Exists is equivalent to wildcard for value, so that a pod can
                                  tolerate all taints of a particular category.
                                  Lt and Gt perform numeric comparisons (requires feature gate TaintTolerationComparisonOperators).
                                type: string
                              tolerationSeconds:
                                description: |-
                                  TolerationSeconds represents the period of time the toleration (which must be
                                  of effect NoExecute, otherwise this field is ignored) tolerates the taint. By default,
                                  it is not set, which means tolerate the taint forever (do not evict). Zero and
                                  negative values will be treated as 0 (evict immediately) by the system.
                                format: int64
                                type: integer
                              value:
                                description: |-
                                  Value is the taint value the toleration matches to.
                                  If the operator is Exists, the value should be empty, otherwise just a regular string.
                                type: string
                            type: object
                          type: array
                        vaultConfigurations:
                          description: |-
                            Vaults are the vault configurations
                            Applicable till CSM v1.14
                          items:
                            description: Vault is the configuration for a vault instance
                              struct
                            properties:
                              address:
                                description: Address is the address for this vault
                                type: string
                              certificateAuthority:
                                description: CertificateAuthority is the base64-encoded
                                  certificate authority for validaitng the vault certificate
                                type: string
                              clientCertificate:
                                description: ClientCertificate is the base64-encoded
                                  certificate for connecting to vault
                                type: string
                              clientKey:
                                description: ClientKey validates is the base64-encoded
                                  certificate key for connecting to vault
                                type: string
                              identifier:
                                description: Identifier is the identifier for this
                                  vault
                                type: string
                              role:
                                description: Role is the role for this vault
                                type: string
                              skipCertificateValidation:
                                description: SkipCertificateValidation validates the
                                  vault server certificate or not
                                type: boolean
                            type: object
                          type: array
                        volumeMounts:
                          description: VolumeMounts is the list of volume mounts for
                            the container
                          items:
                            description: VolumeMount describes a mounting of a Volume
                              within a container.
                            properties:
                              mountPath:
                                description: |-
                                  Path within the container at which the volume should be mounted.  Must
                                  not contain ':'.
                                type: string
                              mountPropagation:
                                description: |-
                                  mountPropagation determines how mounts are propagated from the host
                                  to container and the other way around.
                                  When not set, MountPropagationNone is used.
                                  This field is beta in 1.10.
                                  When RecursiveReadOnly is set to IfPossible or to Enabled, MountPropagation must be None or unspecified
                                  (which defaults to None).
                                type: string
                              name:
                                description: This must match the Name of a Volume.
                                type: string
                              readOnly:
                                description: |-
                                  Mounted read-only if true, read-write otherwise (false or unspecified).
                                  Defaults to false.
                                type: boolean
                              recursiveReadOnly:
                                description: |-
                                  RecursiveReadOnly specifies whether read-only mounts should be handled
                                  recursively.

                                  If ReadOnly is false, this field has no meaning and must be unspecified.

                                  If ReadOnly is true, and this field is set to Disabled, the mount is not made
                                  recursively read-only.  If this field is set to IfPossible, the mount is made
                                  recursively read-only, if it is supported by the container runtime.  If this
                                  field is set to Enabled, the mount is made recursively read-only if it is
                                  supported by the container runtime, otherwise the pod will not be started and
                                  an error will be generated to indicate the reason.

                                  If this field is set to IfPossible or Enabled, MountPropagation must be set to
                                  None (or be unspecified, which defaults to None).

                                  If this field is not specified, it is treated as an equivalent of Disabled.
                                type: string
                              subPath:
                                description: |-
                                  Path within the volume from which the container's volume should be mounted.
                                  Defaults to "" (volume's root).
                                type: string
                              subPathExpr:
                                description: |-
                                  Expanded path within the volume from which the container's volume should be mounted.
                                  Behaves similarly to SubPath but environment variable references $(VAR_NAME) are expanded using the container's environment.
                                  Defaults to "" (volume's root).
                                  SubPathExpr and SubPath are mutually exclusive.
                                type: string
                            required:
                              - mountPath
                              - name
                            type: object
                          type: array
                      type: object
                    replicas:
                      default: 2
                      description: Replicas is the count of controllers for Controller
                        plugin
                      format: int32
                      type: integer
                    sideCars:
                      description: SideCars is the specification for CSI sidecar containers
                      items:
                        description: ContainerTemplate template
                        properties:
                          args:
                            description: Args is the set of arguments for the container
                            items:
                              type: string
                            type: array
                          authorizationController:
                            description: AuthorizationController is the image tag
                              for the container
                            type: string
                          authorizationControllerReplicas:
                            description: AuthorizationControllerReplicas is the number
                              of replicas for the authorization controller deployment
                            type: integer
                          certificate:
                            description: Certificate is a certificate used for a certificate/private-key
                              pair
                            type: string
                          certificateAuthority:
                            description: CertificateAuthority is a certificate authority
                              used to validate a certificate
                            type: string
                          commander:
                            description: Commander is the image tag for the Container
                            type: string
                          configSecretProviderClass:
                            description: |-
                              ConfigSecretProviderClass is the SecretProviderClass Object details for config secret
                              Applicable from CSM v1.15 onwards
                            items:
                              description: ConfigSecretProviderClass is the config
                                secret configuration for CSM Authorization
                              properties:
                                configSecretName:
                                  description: ConfigSecretName is the name of the
                                    Kubernetes secret created by the CSI driver
                                  type: string
                                conjur:
                                  description: Conjur is the secret configuration
                                    with path to retrieve the Config secret from Conjur
                                  properties:
                                    secretPath:
                                      description: SecretPath is the path to the config
                                        secret
                                      type: string
                                  type: object
                                secretProviderClassName:
                                  description: SecretProviderClassName is the name
                                    of the SecretProviderClass that holds the config
                                    secretObject
                                  type: string
                              type: object
                            maxItems: 1
                            type: array
                          controllerReconcileInterval:
                            description: The interval which the reconcile of each
                              controller is run
                            type: string
                          enabled:
                            description: Enabled is used to indicate wether or not
                              to deploy a module
                            type: boolean
                          envs:
                            description: Envs is the set of environment variables
                              for the container
                            items:
                              description: EnvVar represents an environment variable
                                present in a Container.
                              properties:
                                name:
                                  description: |-
                                    Name of the environment variable.
                                    May consist of any printable ASCII characters except '='.
                                  type: string
                                value:
                                  description: |-
                                    Variable references $(VAR_NAME) are expanded
                                    using the previously defined environment variables in the container and
                                    any service environment variables. If a variable cannot be resolved,
                                    the reference in the input string will be unchanged. Double $$ are reduced
                                    to a single $, which allows for escaping the $(VAR_NAME) syntax: i.e.
                                    "$$(VAR_NAME)" will produce the string literal "$(VAR_NAME)".
                                    Escaped references will never be expanded, regardless of whether the variable
                                    exists or not.
                                    Defaults to "".
                                  type: string
                                valueFrom:
                                  description: Source for the environment variable's
                                    value. Cannot be used if value is not empty.
                                  properties:
                                    configMapKeyRef:
                                      description: Selects a key of a ConfigMap.
                                      properties:
                                        key:
                                          description: The key to select.
                                          type: string
                                        name:
                                          default: ""
                                          description: |-
                                            Name of the referent.
                                            This field is effectively required, but due to backwards compatibility is
                                            allowed to be empty. Instances of this type with an empty value here are
                                            almost certainly wrong.
                                            More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                          type: string
                                        optional:
                                          description: Specify whether the ConfigMap
                                            or its key must be defined
                                          type: boolean
                                      required:
                                        - key
                                      type: object
                                      x-kubernetes-map-type: atomic
                                    fieldRef:
                                      description: |-
                                        Selects a field of the pod: supports metadata.name, metadata.namespace, `metadata.labels['<KEY>']`, `metadata.annotations['<KEY>']`,
                                        spec.nodeName, spec.serviceAccountName, status.hostIP, status.podIP, status.podIPs.
                                      properties:
                                        apiVersion:
                                          description: Version of the schema the FieldPath
                                            is written in terms of, defaults to "v1".
                                          type: string
                                        fieldPath:
                                          description: Path of the field to select
                                            in the specified API version.
                                          type: string
                                      required:
                                        - fieldPath
                                      type: object
                                      x-kubernetes-map-type: atomic
                                    fileKeyRef:
                                      description: |-
                                        FileKeyRef selects a key of the env file.
                                        Requires the EnvFiles feature gate to be enabled.
                                      properties:
                                        key:
                                          description: |-
                                            The key within the env file. An invalid key will prevent the pod from starting.
                                            The keys defined within a source may consist of any printable ASCII characters except '='.
                                            During Alpha stage of the EnvFiles feature gate, the key size is limited to 128 characters.
                                          type: string
                                        optional:
                                          default: false
                                          description: |-
                                            Specify whether the file or its key must be defined. If the file or key
                                            does not exist, then the env var is not published.
                                            If optional is set to true and the specified key does not exist,
                                            the environment variable will not be set in the Pod's containers.

                                            If optional is set to false and the specified key does not exist,
                                            an error will be returned during Pod creation.
                                          type: boolean
                                        path:
                                          description: |-
                                            The path within the volume from which to select the file.
                                            Must be relative and may not contain the '..' path or start with '..'.
                                          type: string
                                        volumeName:
                                          description: The name of the volume mount
                                            containing the env file.
                                          type: string
                                      required:
                                        - key
                                        - path
                                        - volumeName
                                      type: object
                                      x-kubernetes-map-type: atomic
                                    resourceFieldRef:
                                      description: |-
                                        Selects a resource of the container: only resources limits and requests
                                        (limits.cpu, limits.memory, limits.ephemeral-storage, requests.cpu, requests.memory and requests.ephemeral-storage) are currently supported.
                                      properties:
                                        containerName:
                                          description: "Container name: required for
                                            volumes, optional for env vars"
                                          type: string
                                        divisor:
                                          anyOf:
                                            - type: integer
                                            - type: string
                                          description: Specifies the output format
                                            of the exposed resources, defaults to
                                            "1"
                                          pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                          x-kubernetes-int-or-string: true
                                        resource:
                                          description: "Required: resource to select"
                                          type: string
                                      required:
                                        - resource
                                      type: object
                                      x-kubernetes-map-type: atomic
                                    secretKeyRef:
                                      description: Selects a key of a secret in the
                                        pod's namespace
                                      properties:
                                        key:
                                          description: The key of the secret to select
                                            from.  Must be a valid secret key.
                                          type: string
                                        name:
                                          default: ""
                                          description: |-
                                            Name of the referent.
                                            This field is effectively required, but due to backwards compatibility is
                                            allowed to be empty. Instances of this type with an empty value here are
                                            almost certainly wrong.
                                            More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                          type: string
                                        optional:
                                          description: Specify whether the Secret
                                            or its key must be defined
                                          type: boolean
                                      required:
                                        - key
                                      type: object
                                      x-kubernetes-map-type: atomic
                                  type: object
                              required:
                                - name
                              type: object
                            maxItems: 30
                            type: array
                          gateway:
                            description: Gateway is the gateway configuration for
                              the authorization proxy-server (v2.5.0+)
                            properties:
                              annotations:
                                additionalProperties:
                                  type: string
                                description: Annotations is an unstructured key value
                                  map that stores additional annotations for the proxy-server
                                  HTTPRoute
                                type: object
                              gatewayClassName:
                                description: GatewayClassName is the name of the GatewayClass
                                  for the proxy-server Gateway resource
                                type: string
                              hosts:
                                description: Hosts is the additional hostnames for
                                  the proxy-server HTTPRoute
                                items:
                                  type: string
                                type: array
                            type: object
                          hostname:
                            description: Hostname is the authorization proxy server
                              hostname
                            type: string
                          image:
                            description: Image is the image tag for the Container
                            type: string
                          imagePullPolicy:
                            description: ImagePullPolicy is the image pull policy
                              for the image
                            type: string
                          leaderElection:
                            description: LeaderElection is boolean flag to enable
                              leader election
                            type: boolean
                          name:
                            description: Name is the name of Container
                            type: string
                          nodeSelector:
                            additionalProperties:
                              type: string
                            description: |-
                              NodeSelector is a selector which must be true for the pod to fit on a node.
                              Selector which must match a node's labels for the pod to be scheduled on that node.
                            type: object
                          opa:
                            description: Opa is the image tag for the Container
                            type: string
                          opaKubeMgmt:
                            description: OpaKubeMgmt is the image tag for the Container
                            type: string
                          openTelemetryCollectorAddress:
                            description: OpenTelemetryCollectorAddress is the address
                              of the OTLP receiving endpoint using gRPC
                            type: string
                          ports:
                            description: Ports is the list of ports for the container
                            items:
                              description: ContainerPort represents a network port
                                in a single container.
                              properties:
                                containerPort:
                                  description: |-
                                    Number of port to expose on the pod's IP address.
                                    This must be a valid port number, 0 < x < 65536.
                                  format: int32
                                  type: integer
                                hostIP:
                                  description: What host IP to bind the external port
                                    to.
                                  type: string
                                hostPort:
                                  description: |-
                                    Number of port to expose on the host.
                                    If specified, this must be a valid port number, 0 < x < 65536.
                                    If HostNetwork is specified, this must match ContainerPort.
                                    Most containers do not need this.
                                  format: int32
                                  type: integer
                                name:
                                  description: |-
                                    If specified, this must be an IANA_SVC_NAME and unique within the pod. Each
                                    named port in a pod must have a unique name. Name for the port that can be
                                    referred to by services.
                                  type: string
                                protocol:
                                  default: TCP
                                  description: |-
                                    Protocol for port. Must be UDP, TCP, or SCTP.
                                    Defaults to "TCP".
                                  type: string
                              required:
                                - containerPort
                              type: object
                            type: array
                          privateKey:
                            description: PrivateKey is a private key used for a certificate/private-key
                              pair
                            type: string
                          proxyServerIngress:
                            description: ProxyServerIngress is the authorization proxy
                              server ingress configuration
                            items:
                              description: ProxyServerIngress is the authorization
                                ingress configuration struct
                              properties:
                                annotations:
                                  additionalProperties:
                                    type: string
                                  description: Annotations is an unstructured key
                                    value map that stores additional annotations for
                                    the ingress
                                  type: object
                                hosts:
                                  description: Hosts is the hosts rules for the ingress
                                  items:
                                    type: string
                                  type: array
                                ingressClassName:
                                  description: IngressClassName is the ingressClassName
                                  type: string
                              type: object
                            type: array
                          proxyService:
                            description: ProxyService is the image tag for the Container
                            type: string
                          proxyServiceReplicas:
                            description: ProxyServiceReplicas is the number of replicas
                              for the proxy service deployment
                            type: integer
                          redis:
                            description: Redis is the image tag for the Container
                            type: string
                          redisCommander:
                            description: RedisCommander is the name of the redis deployment
                            type: string
                          redisName:
                            description: RedisName is the name of the redis statefulset
                            type: string
                          redisPassword:
                            description: RedisPassword is the password for the redis
                              instance
                            type: string
                          redisReplicas:
                            description: RedisReplicas is the number of replicas for
                              the redis deployment
                            type: integer
                          redisSecretProviderClass:
                            description: |-
                              RedisSecretProviderClass is the SecretProviderClass Object details for redis
                              Applicable from CSM v1.15 onwards
                            items:
                              description: RedisSecretProviderClass is the redis secret
                                configuration for CSM Authorization
                              properties:
                                conjur:
                                  description: Conjur is the secret configuration
                                    with path to retrieve the Redis credentials from
                                    Conjur
                                  properties:
                                    passwordPath:
                                      description: PasswordPath is the path to the
                                        password in the secret
                                      type: string
                                    usernamePath:
                                      description: UsernamePath is the path to the
                                        username in the secret
                                      type: string
                                  type: object
                                redisPasswordKey:
                                  description: RedisPasswordKey is the key in the
                                    secret that holds the Redis password
                                  type: string
                                redisSecretName:
                                  description: RedisSecretName is the name of the
                                    Kubernetes secret created by the CSI driver
                                  type: string
                                redisUsernameKey:
                                  description: RedisUsernameKey is the key in the
                                    secret that holds the Redis username
                                  type: string
                                secretProviderClassName:
                                  description: SecretProviderClassName is the name
                                    of the SecretProviderClass that holds the Redis
                                    secretObject
                                  type: string
                              type: object
                            maxItems: 1
                            type: array
                          redisUsername:
                            description: RedisUsername is the username for the redis
                              instance
                            type: string
                          roleService:
                            description: RoleService is the image tag for the Container
                            type: string
                          roleServiceReplicas:
                            description: RoleServiceReplicas is the number of replicas
                              for the role service deployment
                            type: integer
                          secretProviderClasses:
                            description: |-
                              SecretProviderClasses is a collection of secret provider classes for retrieving secrets from external providers for storage system credentials
                              Applicable from CSM v1.15 onwards
                              Only one of SecretProviderClasses or Secrets must be specified (mutually exclusive)
                            properties:
                              conjur:
                                description: Conjur is the list SecretProviderClass
                                  names provided by Conjur
                                items:
                                  properties:
                                    name:
                                      description: Name is the name of the Conjur
                                        SecretProviderClass
                                      type: string
                                    paths:
                                      description: Paths is the list of paths to the
                                        secrets in Conjur
                                      items:
                                        properties:
                                          passwordPath:
                                            description: PasswordPath is the path
                                              to the password in the secret
                                            type: string
                                          usernamePath:
                                            description: UsernamePath is the path
                                              to the username in the secret
                                            type: string
                                        type: object
                                      type: array
                                  type: object
                                type: array
                              vault:
                                description: Vault is the list SecretProviderClass
                                  names provided by Vault
                                items:
                                  type: string
                                type: array
                            type: object
                          secrets:
                            description: |-
                              Secrets is a collection of kubernetes secrets for storage system credentials
                              Applicable from CSM v1.15 onwards
                              Only one of SecretProviderClasses or Secrets must be specified (mutually exclusive)
                            items:
                              type: string
                            type: array
                          sentinel:
                            description: Sentinel is the name of the sentinel statefulSet
                            type: string
                          skipCertificateValidation:
                            description: skipCertificateValidation is the flag to
                              skip certificate validation
                            type: boolean
                          storageService:
                            description: StorageService is the image tag for the Container
                            type: string
                          storageServiceReplicas:
                            description: StorageServiceReplicas is the number of replicas
                              for storage service deployment
                            type: integer
                          tenantService:
                            description: TenantService is the image tag for the Container
                            type: string
                          tenantServiceReplicas:
                            description: TenantServiceReplicas is the number of replicas
                              for the tenant service deployment
                            type: integer
                          tolerations:
                            description: Tolerations is the list of tolerations for
                              the driver pods
                            items:
                              description: |-
                                The pod this Toleration is attached to tolerates any taint that matches
                                the triple <key,value,effect> using the matching operator <operator>.
                              properties:
                                effect:
                                  description: |-
                                    Effect indicates the taint effect to match. Empty means match all taint effects.
                                    When specified, allowed values are NoSchedule, PreferNoSchedule and NoExecute.
                                  type: string
                                key:
                                  description: |-
                                    Key is the taint key that the toleration applies to. Empty means match all taint keys.
                                    If the key is empty, operator must be Exists; this combination means to match all values and all keys.
                                  type: string
                                operator:
                                  description: |-
                                    Operator represents a key's relationship to the value.
                                    Valid operators are Exists, Equal, Lt, and Gt. Defaults to Equal.
                                    Exists is equivalent to wildcard for value, so that a pod can
                                    tolerate all taints of a particular category.
                                    Lt and Gt perform numeric comparisons (requires feature gate TaintTolerationComparisonOperators).
                                  type: string
                                tolerationSeconds:
                                  description: |-
                                    TolerationSeconds represents the period of time the toleration (which must be
                                    of effect NoExecute, otherwise this field is ignored) tolerates the taint. By default,
                                    it is not set, which means tolerate the taint forever (do not evict). Zero and
                                    negative values will be treated as 0 (evict immediately) by the system.
                                  format: int64
                                  type: integer
                                value:
                                  description: |-
                                    Value is the taint value the toleration matches to.
                                    If the operator is Exists, the value should be empty, otherwise just a regular string.
                                  type: string
                              type: object
                            type: array
                          vaultConfigurations:
                            description: |-
                              Vaults are the vault configurations
                              Applicable till CSM v1.14
                            items:
                              description: Vault is the configuration for a vault
                                instance struct
                              properties:
                                address:
                                  description: Address is the address for this vault
                                  type: string
                                certificateAuthority:
                                  description: CertificateAuthority is the base64-encoded
                                    certificate authority for validaitng the vault
                                    certificate
                                  type: string
                                clientCertificate:
                                  description: ClientCertificate is the base64-encoded
                                    certificate for connecting to vault
                                  type: string
                                clientKey:
                                  description: ClientKey validates is the base64-encoded
                                    certificate key for connecting to vault
                                  type: string
                                identifier:
                                  description: Identifier is the identifier for this
                                    vault
                                  type: string
                                role:
                                  description: Role is the role for this vault
                                  type: string
                                skipCertificateValidation:
                                  description: SkipCertificateValidation validates
                                    the vault server certificate or not
                                  type: boolean
                              type: object
                            type: array
                          volumeMounts:
                            description: VolumeMounts is the list of volume mounts
                              for the container
                            items:
                              description: VolumeMount describes a mounting of a Volume
                                within a container.
                              properties:
                                mountPath:
                                  description: |-
                                    Path within the container at which the volume should be mounted.  Must
                                    not contain ':'.
                                  type: string
                                mountPropagation:
                                  description: |-
                                    mountPropagation determines how mounts are propagated from the host
                                    to container and the other way around.
                                    When not set, MountPropagationNone is used.
                                    This field is beta in 1.10.
                                    When RecursiveReadOnly is set to IfPossible or to Enabled, MountPropagation must be None or unspecified
                                    (which defaults to None).
                                  type: string
                                name:
                                  description: This must match the Name of a Volume.
                                  type: string
                                readOnly:
                                  description: |-
                                    Mounted read-only if true, read-write otherwise (false or unspecified).
                                    Defaults to false.
                                  type: boolean
                                recursiveReadOnly:
                                  description: |-
                                    RecursiveReadOnly specifies whether read-only mounts should be handled
                                    recursively.

                                    If ReadOnly is false, this field has no meaning and must be unspecified.

                                    If ReadOnly is true, and this field is set to Disabled, the mount is not made
                                    recursively read-only.  If this field is set to IfPossible, the mount is made
                                    recursively read-only, if it is supported by the container runtime.  If this
                                    field is set to Enabled, the mount is made recursively read-only if it is
                                    supported by the container runtime, otherwise the pod will not be started and
                                    an error will be generated to indicate the reason.

                                    If this field is set to IfPossible or Enabled, MountPropagation must be set to
                                    None (or be unspecified, which defaults to None).

                                    If this field is not specified, it is treated as an equivalent of Disabled.
                                  type: string
                                subPath:
                                  description: |-
                                    Path within the volume from which the container's volume should be mounted.
                                    Defaults to "" (volume's root).
                                  type: string
                                subPathExpr:
                                  description: |-
                                    Expanded path within the volume from which the container's volume should be mounted.
                                    Behaves similarly to SubPath but environment variable references $(VAR_NAME) are expanded using the container's environment.
                                    Defaults to "" (volume's root).
                                    SubPathExpr and SubPath are mutually exclusive.
                                  type: string
                              required:
                                - mountPath
                                - name
                              type: object
                            type: array
                        type: object
                      maxItems: 20
                      type: array
                    snapshotClass:
                      description: SnapshotClass is the specification for Snapshot
                        Classes
                      items:
                        description: SnapshotClass struct
                        properties:
                          name:
                            description: Name is the name of the Snapshot Class
                            type: string
                          parameters:
                            additionalProperties:
                              type: string
                            description: Parameters is a map of driver specific parameters
                              for snapshot class
                            type: object
                        type: object
                      type: array
                    tlsCertSecret:
                      description: TLSCertSecret is the name of the TLS Cert secret
                      type: string
                  type: object
                modules:
                  description: Modules is list of Container Storage Module modules
                    you want to deploy
                  items:
                    description: Module defines the desired state of a ContainerStorageModule
                    maxProperties: 11
                    properties:
                      components:
                        description: Components is the specification for CSM components
                          containers
                        items:
                          description: ContainerTemplate template
                          properties:
                            args:
                              description: Args is the set of arguments for the container
                              items:
                                type: string
                              type: array
                            authorizationController:
                              description: AuthorizationController is the image tag
                                for the container
                              type: string
                            authorizationControllerReplicas:
                              description: AuthorizationControllerReplicas is the
                                number of replicas for the authorization controller
                                deployment
                              type: integer
                            certificate:
                              description: Certificate is a certificate used for a
                                certificate/private-key pair
                              type: string
                            certificateAuthority:
                              description: CertificateAuthority is a certificate authority
                                used to validate a certificate
                              type: string
                            commander:
                              description: Commander is the image tag for the Container
                              type: string
                            configSecretProviderClass:
                              description: |-
                                ConfigSecretProviderClass is the SecretProviderClass Object details for config secret
                                Applicable from CSM v1.15 onwards
                              items:
                                description: ConfigSecretProviderClass is the config
                                  secret configuration for CSM Authorization
                                properties:
                                  configSecretName:
                                    description: ConfigSecretName is the name of the
                                      Kubernetes secret created by the CSI driver
                                    type: string
                                  conjur:
                                    description: Conjur is the secret configuration
                                      with path to retrieve the Config secret from
                                      Conjur
                                    properties:
                                      secretPath:
                                        description: SecretPath is the path to the
                                          config secret
                                        type: string
                                    type: object
                                  secretProviderClassName:
                                    description: SecretProviderClassName is the name
                                      of the SecretProviderClass that holds the config
                                      secretObject
                                    type: string
                                type: object
                              maxItems: 1
                              type: array
                            controllerReconcileInterval:
                              description: The interval which the reconcile of each
                                controller is run
                              type: string
                            enabled:
                              description: Enabled is used to indicate wether or not
                                to deploy a module
                              type: boolean
                            envs:
                              description: Envs is the set of environment variables
                                for the container
                              items:
                                description: EnvVar represents an environment variable
                                  present in a Container.
                                properties:
                                  name:
                                    description: |-
                                      Name of the environment variable.
                                      May consist of any printable ASCII characters except '='.
                                    type: string
                                  value:
                                    description: |-
                                      Variable references $(VAR_NAME) are expanded
                                      using the previously defined environment variables in the container and
                                      any service environment variables. If a variable cannot be resolved,
                                      the reference in the input string will be unchanged. Double $$ are reduced
                                      to a single $, which allows for escaping the $(VAR_NAME) syntax: i.e.
                                      "$$(VAR_NAME)" will produce the string literal "$(VAR_NAME)".
                                      Escaped references will never be expanded, regardless of whether the variable
                                      exists or not.
                                      Defaults to "".
                                    type: string
                                  valueFrom:
                                    description: Source for the environment variable's
                                      value. Cannot be used if value is not empty.
                                    properties:
                                      configMapKeyRef:
                                        description: Selects a key of a ConfigMap.
                                        properties:
                                          key:
                                            description: The key to select.
                                            type: string
                                          name:
                                            default: ""
                                            description: |-
                                              Name of the referent.
                                              This field is effectively required, but due to backwards compatibility is
                                              allowed to be empty. Instances of this type with an empty value here are
                                              almost certainly wrong.
                                              More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                            type: string
                                          optional:
                                            description: Specify whether the ConfigMap
                                              or its key must be defined
                                            type: boolean
                                        required:
                                          - key
                                        type: object
                                        x-kubernetes-map-type: atomic
                                      fieldRef:
                                        description: |-
                                          Selects a field of the pod: supports metadata.name, metadata.namespace, `metadata.labels['<KEY>']`, `metadata.annotations['<KEY>']`,
                                          spec.nodeName, spec.serviceAccountName, status.hostIP, status.podIP, status.podIPs.
                                        properties:
                                          apiVersion:
                                            description: Version of the schema the
                                              FieldPath is written in terms of, defaults
                                              to "v1".
                                            type: string
                                          fieldPath:
                                            description: Path of the field to select
                                              in the specified API version.
                                            type: string
                                        required:
                                          - fieldPath
                                        type: object
                                        x-kubernetes-map-type: atomic
                                      fileKeyRef:
                                        description: |-
                                          FileKeyRef selects a key of the env file.
                                          Requires the EnvFiles feature gate to be enabled.
                                        properties:
                                          key:
                                            description: |-
                                              The key within the env file. An invalid key will prevent the pod from starting.
                                              The keys defined within a source may consist of any printable ASCII characters except '='.
                                              During Alpha stage of the EnvFiles feature gate, the key size is limited to 128 characters.
                                            type: string
                                          optional:
                                            default: false
                                            description: |-
                                              Specify whether the file or its key must be defined. If the file or key
                                              does not exist, then the env var is not published.
                                              If optional is set to true and the specified key does not exist,
                                              the environment variable will not be set in the Pod's containers.

                                              If optional is set to false and the specified key does not exist,
                                              an error will be returned during Pod creation.
                                            type: boolean
                                          path:
                                            description: |-
                                              The path within the volume from which to select the file.
                                              Must be relative and may not contain the '..' path or start with '..'.
                                            type: string
                                          volumeName:
                                            description: The name of the volume mount
                                              containing the env file.
                                            type: string
                                        required:
                                          - key
                                          - path
                                          - volumeName
                                        type: object
                                        x-kubernetes-map-type: atomic
                                      resourceFieldRef:
                                        description: |-
                                          Selects a resource of the container: only resources limits and requests
                                          (limits.cpu, limits.memory, limits.ephemeral-storage, requests.cpu, requests.memory and requests.ephemeral-storage) are currently supported.
                                        properties:
                                          containerName:
                                            description: "Container name: required
                                              for volumes, optional for env vars"
                                            type: string
                                          divisor:
                                            anyOf:
                                              - type: integer
                                              - type: string
                                            description: Specifies the output format
                                              of the exposed resources, defaults to
                                              "1"
                                            pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                            x-kubernetes-int-or-string: true
                                          resource:
                                            description: "Required: resource to select"
                                            type: string
                                        required:
                                          - resource
                                        type: object
                                        x-kubernetes-map-type: atomic
                                      secretKeyRef:
                                        description: Selects a key of a secret in
                                          the pod's namespace
                                        properties:
                                          key:
                                            description: The key of the secret to
                                              select from.  Must be a valid secret
                                              key.
                                            type: string
                                          name:
                                            default: ""
                                            description: |-
                                              Name of the referent.
                                              This field is effectively required, but due to backwards compatibility is
                                              allowed to be empty. Instances of this type with an empty value here are
                                              almost certainly wrong.
                                              More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                            type: string
                                          optional:
                                            description: Specify whether the Secret
                                              or its key must be defined
                                            type: boolean
                                        required:
                                          - key
                                        type: object
                                        x-kubernetes-map-type: atomic
                                    type: object
                                required:
                                  - name
                                type: object
                              maxItems: 30
                              type: array
                            gateway:
                              description: Gateway is the gateway configuration for
                                the authorization proxy-server (v2.5.0+)
                              properties:
                                annotations:
                                  additionalProperties:
                                    type: string
                                  description: Annotations is an unstructured key
                                    value map that stores additional annotations for
                                    the proxy-server HTTPRoute
                                  type: object
                                gatewayClassName:
                                  description: GatewayClassName is the name of the
                                    GatewayClass for the proxy-server Gateway resource
                                  type: string
                                hosts:
                                  description: Hosts is the additional hostnames for
                                    the proxy-server HTTPRoute
                                  items:
                                    type: string
                                  type: array
                              type: object
                            hostname:
                              description: Hostname is the authorization proxy server
                                hostname
                              type: string
                            image:
                              description: Image is the image tag for the Container
                              type: string
                            imagePullPolicy:
                              description: ImagePullPolicy is the image pull policy
                                for the image
                              type: string
                            leaderElection:
                              description: LeaderElection is boolean flag to enable
                                leader election
                              type: boolean
                            name:
                              description: Name is the name of Container
                              type: string
                            nodeSelector:
                              additionalProperties:
                                type: string
                              description: |-
                                NodeSelector is a selector which must be true for the pod to fit on a node.
                                Selector which must match a node's labels for the pod to be scheduled on that node.
                              type: object
                            opa:
                              description: Opa is the image tag for the Container
                              type: string
                            opaKubeMgmt:
                              description: OpaKubeMgmt is the image tag for the Container
                              type: string
                            openTelemetryCollectorAddress:
                              description: OpenTelemetryCollectorAddress is the address
                                of the OTLP receiving endpoint using gRPC
                              type: string
                            ports:
                              description: Ports is the list of ports for the container
                              items:
                                description: ContainerPort represents a network port
                                  in a single container.
                                properties:
                                  containerPort:
                                    description: |-
                                      Number of port to expose on the pod's IP address.
                                      This must be a valid port number, 0 < x < 65536.
                                    format: int32
                                    type: integer
                                  hostIP:
                                    description: What host IP to bind the external
                                      port to.
                                    type: string
                                  hostPort:
                                    description: |-
                                      Number of port to expose on the host.
                                      If specified, this must be a valid port number, 0 < x < 65536.
                                      If HostNetwork is specified, this must match ContainerPort.
                                      Most containers do not need this.
                                    format: int32
                                    type: integer
                                  name:
                                    description: |-
                                      If specified, this must be an IANA_SVC_NAME and unique within the pod. Each
                                      named port in a pod must have a unique name. Name for the port that can be
                                      referred to by services.
                                    type: string
                                  protocol:
                                    default: TCP
                                    description: |-
                                      Protocol for port. Must be UDP, TCP, or SCTP.
                                      Defaults to "TCP".
                                    type: string
                                required:
                                  - containerPort
                                type: object
                              type: array
                            privateKey:
                              description: PrivateKey is a private key used for a
                                certificate/private-key pair
                              type: string
                            proxyServerIngress:
                              description: ProxyServerIngress is the authorization
                                proxy server ingress configuration
                              items:
                                description: ProxyServerIngress is the authorization
                                  ingress configuration struct
                                properties:
                                  annotations:
                                    additionalProperties:
                                      type: string
                                    description: Annotations is an unstructured key
                                      value map that stores additional annotations
                                      for the ingress
                                    type: object
                                  hosts:
                                    description: Hosts is the hosts rules for the
                                      ingress
                                    items:
                                      type: string
                                    type: array
                                  ingressClassName:
                                    description: IngressClassName is the ingressClassName
                                    type: string
                                type: object
                              type: array
                            proxyService:
                              description: ProxyService is the image tag for the Container
                              type: string
                            proxyServiceReplicas:
                              description: ProxyServiceReplicas is the number of replicas
                                for the proxy service deployment
                              type: integer
                            redis:
                              description: Redis is the image tag for the Container
                              type: string
                            redisCommander:
                              description: RedisCommander is the name of the redis
                                deployment
                              type: string
                            redisName:
                              description: RedisName is the name of the redis statefulset
                              type: string
                            redisPassword:
                              description: RedisPassword is the password for the redis
                                instance
                              type: string
                            redisReplicas:
                              description: RedisReplicas is the number of replicas
                                for the redis deployment
                              type: integer
                            redisSecretProviderClass:
                              description: |-
                                RedisSecretProviderClass is the SecretProviderClass Object details for redis
                                Applicable from CSM v1.15 onwards
                              items:
                                description: RedisSecretProviderClass is the redis
                                  secret configuration for CSM Authorization
                                properties:
                                  conjur:
                                    description: Conjur is the secret configuration
                                      with path to retrieve the Redis credentials
                                      from Conjur
                                    properties:
                                      passwordPath:
                                        description: PasswordPath is the path to the
                                          password in the secret
                                        type: string
                                      usernamePath:
                                        description: UsernamePath is the path to the
                                          username in the secret
                                        type: string
                                    type: object
                                  redisPasswordKey:
                                    description: RedisPasswordKey is the key in the
                                      secret that holds the Redis password
                                    type: string
                                  redisSecretName:
                                    description: RedisSecretName is the name of the
                                      Kubernetes secret created by the CSI driver
                                    type: string
                                  redisUsernameKey:
                                    description: RedisUsernameKey is the key in the
                                      secret that holds the Redis username
                                    type: string
                                  secretProviderClassName:
                                    description: SecretProviderClassName is the name
                                      of the SecretProviderClass that holds the Redis
                                      secretObject
                                    type: string
                                type: object
                              maxItems: 1
                              type: array
                            redisUsername:
                              description: RedisUsername is the username for the redis
                                instance
                              type: string
                            roleService:
                              description: RoleService is the image tag for the Container
                              type: string
                            roleServiceReplicas:
                              description: RoleServiceReplicas is the number of replicas
                                for the role service deployment
                              type: integer
                            secretProviderClasses:
                              description: |-
                                SecretProviderClasses is a collection of secret provider classes for retrieving secrets from external providers for storage system credentials
                                Applicable from CSM v1.15 onwards
                                Only one of SecretProviderClasses or Secrets must be specified (mutually exclusive)
                              properties:
                                conjur:
                                  description: Conjur is the list SecretProviderClass
                                    names provided by Conjur
                                  items:
                                    properties:
                                      name:
                                        description: Name is the name of the Conjur
                                          SecretProviderClass
                                        type: string
                                      paths:
                                        description: Paths is the list of paths to
                                          the secrets in Conjur
                                        items:
                                          properties:
                                            passwordPath:
                                              description: PasswordPath is the path
                                                to the password in the secret
                                              type: string
                                            usernamePath:
                                              description: UsernamePath is the path
                                                to the username in the secret
                                              type: string
                                          type: object
                                        type: array
                                    type: object
                                  type: array
                                vault:
                                  description: Vault is the list SecretProviderClass
                                    names provided by Vault
                                  items:
                                    type: string
                                  type: array
                              type: object
                            secrets:
                              description: |-
                                Secrets is a collection of kubernetes secrets for storage system credentials
                                Applicable from CSM v1.15 onwards
                                Only one of SecretProviderClasses or Secrets must be specified (mutually exclusive)
                              items:
                                type: string
                              type: array
                            sentinel:
                              description: Sentinel is the name of the sentinel statefulSet
                              type: string
                            skipCertificateValidation:
                              description: skipCertificateValidation is the flag to
                                skip certificate validation
                              type: boolean
                            storageService:
                              description: StorageService is the image tag for the
                                Container
                              type: string
                            storageServiceReplicas:
                              description: StorageServiceReplicas is the number of
                                replicas for storage service deployment
                              type: integer
                            tenantService:
                              description: TenantService is the image tag for the
                                Container
                              type: string
                            tenantServiceReplicas:
                              description: TenantServiceReplicas is the number of
                                replicas for the tenant service deployment
                              type: integer
                            tolerations:
                              description: Tolerations is the list of tolerations
                                for the driver pods
                              items:
                                description: |-
                                  The pod this Toleration is attached to tolerates any taint that matches
                                  the triple <key,value,effect> using the matching operator <operator>.
                                properties:
                                  effect:
                                    description: |-
                                      Effect indicates the taint effect to match. Empty means match all taint effects.
                                      When specified, allowed values are NoSchedule, PreferNoSchedule and NoExecute.
                                    type: string
                                  key:
                                    description: |-
                                      Key is the taint key that the toleration applies to. Empty means match all taint keys.
                                      If the key is empty, operator must be Exists; this combination means to match all values and all keys.
                                    type: string
                                  operator:
                                    description: |-
                                      Operator represents a key's relationship to the value.
                                      Valid operators are Exists, Equal, Lt, and Gt. Defaults to Equal.
                                      Exists is equivalent to wildcard for value, so that a pod can
                                      tolerate all taints of a particular category.
                                      Lt and Gt perform numeric comparisons (requires feature gate TaintTolerationComparisonOperators).
                                    type: string
                                  tolerationSeconds:
                                    description: |-
                                      TolerationSeconds represents the period of time the toleration (which must be
                                      of effect NoExecute, otherwise this field is ignored) tolerates the taint. By default,
                                      it is not set, which means tolerate the taint forever (do not evict). Zero and
                                      negative values will be treated as 0 (evict immediately) by the system.
                                    format: int64
                                    type: integer
                                  value:
                                    description: |-
                                      Value is the taint value the toleration matches to.
                                      If the operator is Exists, the value should be empty, otherwise just a regular string.
                                    type: string
                                type: object
                              type: array
                            vaultConfigurations:
                              description: |-
                                Vaults are the vault configurations
                                Applicable till CSM v1.14
                              items:
                                description: Vault is the configuration for a vault
                                  instance struct
                                properties:
                                  address:
                                    description: Address is the address for this vault
                                    type: string
                                  certificateAuthority:
                                    description: CertificateAuthority is the base64-encoded
                                      certificate authority for validaitng the vault
                                      certificate
                                    type: string
                                  clientCertificate:
                                    description: ClientCertificate is the base64-encoded
                                      certificate for connecting to vault
                                    type: string
                                  clientKey:
                                    description: ClientKey validates is the base64-encoded
                                      certificate key for connecting to vault
                                    type: string
                                  identifier:
                                    description: Identifier is the identifier for
                                      this vault
                                    type: string
                                  role:
                                    description: Role is the role for this vault
                                    type: string
                                  skipCertificateValidation:
                                    description: SkipCertificateValidation validates
                                      the vault server certificate or not
                                    type: boolean
                                type: object
                              type: array
                            volumeMounts:
                              description: VolumeMounts is the list of volume mounts
                                for the container
                              items:
                                description: VolumeMount describes a mounting of a
                                  Volume within a container.
                                properties:
                                  mountPath:
                                    description: |-
                                      Path within the container at which the volume should be mounted.  Must
                                      not contain ':'.
                                    type: string
                                  mountPropagation:
                                    description: |-
                                      mountPropagation determines how mounts are propagated from the host
                                      to container and the other way around.
                                      When not set, MountPropagationNone is used.
                                      This field is beta in 1.10.
                                      When RecursiveReadOnly is set to IfPossible or to Enabled, MountPropagation must be None or unspecified
                                      (which defaults to None).
                                    type: string
                                  name:
                                    description: This must match the Name of a Volume.
                                    type: string
                                  readOnly:
                                    description: |-
                                      Mounted read-only if true, read-write otherwise (false or unspecified).
                                      Defaults to false.
                                    type: boolean
                                  recursiveReadOnly:
                                    description: |-
                                      RecursiveReadOnly specifies whether read-only mounts should be handled
                                      recursively.

                                      If ReadOnly is false, this field has no meaning and must be unspecified.

                                      If ReadOnly is true, and this field is set to Disabled, the mount is not made
                                      recursively read-only.  If this field is set to IfPossible, the mount is made
                                      recursively read-only, if it is supported by the container runtime.  If this
                                      field is set to Enabled, the mount is made recursively read-only if it is
                                      supported by the container runtime, otherwise the pod will not be started and
                                      an error will be generated to indicate the reason.

                                      If this field is set to IfPossible or Enabled, MountPropagation must be set to
                                      None (or be unspecified, which defaults to None).

                                      If this field is not specified, it is treated as an equivalent of Disabled.
                                    type: string
                                  subPath:
                                    description: |-
                                      Path within the volume from which the container's volume should be mounted.
                                      Defaults to "" (volume's root).
                                    type: string
                                  subPathExpr:
                                    description: |-
                                      Expanded path within the volume from which the container's volume should be mounted.
                                      Behaves similarly to SubPath but environment variable references $(VAR_NAME) are expanded using the container's environment.
                                      Defaults to "" (volume's root).
                                      SubPathExpr and SubPath are mutually exclusive.
                                    type: string
                                required:
                                  - mountPath
                                  - name
                                type: object
                              type: array
                          type: object
                        maxItems: 20
                        type: array
                      configVersion:
                        description: |-
                          ConfigVersion is the configuration version of the module
                          Deprecated: Use spec.version instead. This field will be removed in a future release.
                        type: string
                      enabled:
                        description: Enabled is used to indicate whether or not to
                          deploy a module
                        type: boolean
                      forceRemoveModule:
                        description: ForceRemoveModule is the boolean flag used to
                          remove authorization proxy server deployment when CR is
                          deleted
                        type: boolean
                      initContainer:
                        description: InitContainer is the specification for Module
                          InitContainer
                        items:
                          description: ContainerTemplate template
                          properties:
                            args:
                              description: Args is the set of arguments for the container
                              items:
                                type: string
                              type: array
                            authorizationController:
                              description: AuthorizationController is the image tag
                                for the container
                              type: string
                            authorizationControllerReplicas:
                              description: AuthorizationControllerReplicas is the
                                number of replicas for the authorization controller
                                deployment
                              type: integer
                            certificate:
                              description: Certificate is a certificate used for a
                                certificate/private-key pair
                              type: string
                            certificateAuthority:
                              description: CertificateAuthority is a certificate authority
                                used to validate a certificate
                              type: string
                            commander:
                              description: Commander is the image tag for the Container
                              type: string
                            configSecretProviderClass:
                              description: |-
                                ConfigSecretProviderClass is the SecretProviderClass Object details for config secret
                                Applicable from CSM v1.15 onwards
                              items:
                                description: ConfigSecretProviderClass is the config
                                  secret configuration for CSM Authorization
                                properties:
                                  configSecretName:
                                    description: ConfigSecretName is the name of the
                                      Kubernetes secret created by the CSI driver
                                    type: string
                                  conjur:
                                    description: Conjur is the secret configuration
                                      with path to retrieve the Config secret from
                                      Conjur
                                    properties:
                                      secretPath:
                                        description: SecretPath is the path to the
                                          config secret
                                        type: string
                                    type: object
                                  secretProviderClassName:
                                    description: SecretProviderClassName is the name
                                      of the SecretProviderClass that holds the config
                                      secretObject
                                    type: string
                                type: object
                              maxItems: 1
                              type: array
                            controllerReconcileInterval:
                              description: The interval which the reconcile of each
                                controller is run
                              type: string
                            enabled:
                              description: Enabled is used to indicate wether or not
                                to deploy a module
                              type: boolean
                            envs:
                              description: Envs is the set of environment variables
                                for the container
                              items:
                                description: EnvVar represents an environment variable
                                  present in a Container.
                                properties:
                                  name:
                                    description: |-
                                      Name of the environment variable.
                                      May consist of any printable ASCII characters except '='.
                                    type: string
                                  value:
                                    description: |-
                                      Variable references $(VAR_NAME) are expanded
                                      using the previously defined environment variables in the container and
                                      any service environment variables. If a variable cannot be resolved,
                                      the reference in the input string will be unchanged. Double $$ are reduced
                                      to a single $, which allows for escaping the $(VAR_NAME) syntax: i.e.
                                      "$$(VAR_NAME)" will produce the string literal "$(VAR_NAME)".
                                      Escaped references will never be expanded, regardless of whether the variable
                                      exists or not.
                                      Defaults to "".
                                    type: string
                                  valueFrom:
                                    description: Source for the environment variable's
                                      value. Cannot be used if value is not empty.
                                    properties:
                                      configMapKeyRef:
                                        description: Selects a key of a ConfigMap.
                                        properties:
                                          key:
                                            description: The key to select.
                                            type: string
                                          name:
                                            default: ""
                                            description: |-
                                              Name of the referent.
                                              This field is effectively required, but due to backwards compatibility is
                                              allowed to be empty. Instances of this type with an empty value here are
                                              almost certainly wrong.
                                              More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                            type: string
                                          optional:
                                            description: Specify whether the ConfigMap
                                              or its key must be defined
                                            type: boolean
                                        required:
                                          - key
                                        type: object
                                        x-kubernetes-map-type: atomic
                                      fieldRef:
                                        description: |-
                                          Selects a field of the pod: supports metadata.name, metadata.namespace, `metadata.labels['<KEY>']`, `metadata.annotations['<KEY>']`,
                                          spec.nodeName, spec.serviceAccountName, status.hostIP, status.podIP, status.podIPs.
                                        properties:
                                          apiVersion:
                                            description: Version of the schema the
                                              FieldPath is written in terms of, defaults
                                              to "v1".
                                            type: string
                                          fieldPath:
                                            description: Path of the field to select
                                              in the specified API version.
                                            type: string
                                        required:
                                          - fieldPath
                                        type: object
                                        x-kubernetes-map-type: atomic
                                      fileKeyRef:
                                        description: |-
                                          FileKeyRef selects a key of the env file.
                                          Requires the EnvFiles feature gate to be enabled.
                                        properties:
                                          key:
                                            description: |-
                                              The key within the env file. An invalid key will prevent the pod from starting.
                                              The keys defined within a source may consist of any printable ASCII characters except '='.
                                              During Alpha stage of the EnvFiles feature gate, the key size is limited to 128 characters.
                                            type: string
                                          optional:
                                            default: false
                                            description: |-
                                              Specify whether the file or its key must be defined. If the file or key
                                              does not exist, then the env var is not published.
                                              If optional is set to true and the specified key does not exist,
                                              the environment variable will not be set in the Pod's containers.

                                              If optional is set to false and the specified key does not exist,
                                              an error will be returned during Pod creation.
                                            type: boolean
                                          path:
                                            description: |-
                                              The path within the volume from which to select the file.
                                              Must be relative and may not contain the '..' path or start with '..'.
                                            type: string
                                          volumeName:
                                            description: The name of the volume mount
                                              containing the env file.
                                            type: string
                                        required:
                                          - key
                                          - path
                                          - volumeName
                                        type: object
                                        x-kubernetes-map-type: atomic
                                      resourceFieldRef:
                                        description: |-
                                          Selects a resource of the container: only resources limits and requests
                                          (limits.cpu, limits.memory, limits.ephemeral-storage, requests.cpu, requests.memory and requests.ephemeral-storage) are currently supported.
                                        properties:
                                          containerName:
                                            description: "Container name: required
                                              for volumes, optional for env vars"
                                            type: string
                                          divisor:
                                            anyOf:
                                              - type: integer
                                              - type: string
                                            description: Specifies the output format
                                              of the exposed resources, defaults to
                                              "1"
                                            pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                            x-kubernetes-int-or-string: true
                                          resource:
                                            description: "Required: resource to select"
                                            type: string
                                        required:
                                          - resource
                                        type: object
                                        x-kubernetes-map-type: atomic
                                      secretKeyRef:
                                        description: Selects a key of a secret in
                                          the pod's namespace
                                        properties:
                                          key:
                                            description: The key of the secret to
                                              select from.  Must be a valid secret
                                              key.
                                            type: string
                                          name:
                                            default: ""
                                            description: |-
                                              Name of the referent.
                                              This field is effectively required, but due to backwards compatibility is
                                              allowed to be empty. Instances of this type with an empty value here are
                                              almost certainly wrong.
                                              More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                            type: string
                                          optional:
                                            description: Specify whether the Secret
                                              or its key must be defined
                                            type: boolean
                                        required:
                                          - key
                                        type: object
                                        x-kubernetes-map-type: atomic
                                    type: object
                                required:
                                  - name
                                type: object
                              maxItems: 30
                              type: array
                            gateway:
                              description: Gateway is the gateway configuration for
                                the authorization proxy-server (v2.5.0+)
                              properties:
                                annotations:
                                  additionalProperties:
                                    type: string
                                  description: Annotations is an unstructured key
                                    value map that stores additional annotations for
                                    the proxy-server HTTPRoute
                                  type: object
                                gatewayClassName:
                                  description: GatewayClassName is the name of the
                                    GatewayClass for the proxy-server Gateway resource
                                  type: string
                                hosts:
                                  description: Hosts is the additional hostnames for
                                    the proxy-server HTTPRoute
                                  items:
                                    type: string
                                  type: array
                              type: object
                            hostname:
                              description: Hostname is the authorization proxy server
                                hostname
                              type: string
                            image:
                              description: Image is the image tag for the Container
                              type: string
                            imagePullPolicy:
                              description: ImagePullPolicy is the image pull policy
                                for the image
                              type: string
                            leaderElection:
                              description: LeaderElection is boolean flag to enable
                                leader election
                              type: boolean
                            name:
                              description: Name is the name of Container
                              type: string
                            nodeSelector:
                              additionalProperties:
                                type: string
                              description: |-
                                NodeSelector is a selector which must be true for the pod to fit on a node.
                                Selector which must match a node's labels for the pod to be scheduled on that node.
                              type: object
                            opa:
                              description: Opa is the image tag for the Container
                              type: string
                            opaKubeMgmt:
                              description: OpaKubeMgmt is the image tag for the Container
                              type: string
                            openTelemetryCollectorAddress:
                              description: OpenTelemetryCollectorAddress is the address
                                of the OTLP receiving endpoint using gRPC
                              type: string
                            ports:
                              description: Ports is the list of ports for the container
                              items:
                                description: ContainerPort represents a network port
                                  in a single container.
                                properties:
                                  containerPort:
                                    description: |-
                                      Number of port to expose on the pod's IP address.
                                      This must be a valid port number, 0 < x < 65536.
                                    format: int32
                                    type: integer
                                  hostIP:
                                    description: What host IP to bind the external
                                      port to.
                                    type: string
                                  hostPort:
                                    description: |-
                                      Number of port to expose on the host.
                                      If specified, this must be a valid port number, 0 < x < 65536.
                                      If HostNetwork is specified, this must match ContainerPort.
                                      Most containers do not need this.
                                    format: int32
                                    type: integer
                                  name:
                                    description: |-
                                      If specified, this must be an IANA_SVC_NAME and unique within the pod. Each
                                      named port in a pod must have a unique name. Name for the port that can be
                                      referred to by services.
                                    type: string
                                  protocol:
                                    default: TCP
                                    description: |-
                                      Protocol for port. Must be UDP, TCP, or SCTP.
                                      Defaults to "TCP".
                                    type: string
                                required:
                                  - containerPort
                                type: object
                              type: array
                            privateKey:
                              description: PrivateKey is a private key used for a
                                certificate/private-key pair
                              type: string
                            proxyServerIngress:
                              description: ProxyServerIngress is the authorization
                                proxy server ingress configuration
                              items:
                                description: ProxyServerIngress is the authorization
                                  ingress configuration struct
                                properties:
                                  annotations:
                                    additionalProperties:
                                      type: string
                                    description: Annotations is an unstructured key
                                      value map that stores additional annotations
                                      for the ingress
                                    type: object
                                  hosts:
                                    description: Hosts is the hosts rules for the
                                      ingress
                                    items:
                                      type: string
                                    type: array
                                  ingressClassName:
                                    description: IngressClassName is the ingressClassName
                                    type: string
                                type: object
                              type: array
                            proxyService:
                              description: ProxyService is the image tag for the Container
                              type: string
                            proxyServiceReplicas:
                              description: ProxyServiceReplicas is the number of replicas
                                for the proxy service deployment
                              type: integer
                            redis:
                              description: Redis is the image tag for the Container
                              type: string
                            redisCommander:
                              description: RedisCommander is the name of the redis
                                deployment
                              type: string
                            redisName:
                              description: RedisName is the name of the redis statefulset
                              type: string
                            redisPassword:
                              description: RedisPassword is the password for the redis
                                instance
                              type: string
                            redisReplicas:
                              description: RedisReplicas is the number of replicas
                                for the redis deployment
                              type: integer
                            redisSecretProviderClass:
                              description: |-
                                RedisSecretProviderClass is the SecretProviderClass Object details for redis
                                Applicable from CSM v1.15 onwards
                              items:
                                description: RedisSecretProviderClass is the redis
                                  secret configuration for CSM Authorization
                                properties:
                                  conjur:
                                    description: Conjur is the secret configuration
                                      with path to retrieve the Redis credentials
                                      from Conjur
                                    properties:
                                      passwordPath:
                                        description: PasswordPath is the path to the
                                          password in the secret
                                        type: string
                                      usernamePath:
                                        description: UsernamePath is the path to the
                                          username in the secret
                                        type: string
                                    type: object
                                  redisPasswordKey:
                                    description: RedisPasswordKey is the key in the
                                      secret that holds the Redis password
                                    type: string
                                  redisSecretName:
                                    description: RedisSecretName is the name of the
                                      Kubernetes secret created by the CSI driver
                                    type: string
                                  redisUsernameKey:
                                    description: RedisUsernameKey is the key in the
                                      secret that holds the Redis username
                                    type: string
                                  secretProviderClassName:
                                    description: SecretProviderClassName is the name
                                      of the SecretProviderClass that holds the Redis
                                      secretObject
                                    type: string
                                type: object
                              maxItems: 1
                              type: array
                            redisUsername:
                              description: RedisUsername is the username for the redis
                                instance
                              type: string
                            roleService:
                              description: RoleService is the image tag for the Container
                              type: string
                            roleServiceReplicas:
                              description: RoleServiceReplicas is the number of replicas
                                for the role service deployment
                              type: integer
                            secretProviderClasses:
                              description: |-
                                SecretProviderClasses is a collection of secret provider classes for retrieving secrets from external providers for storage system credentials
                                Applicable from CSM v1.15 onwards
                                Only one of SecretProviderClasses or Secrets must be specified (mutually exclusive)
                              properties:
                                conjur:
                                  description: Conjur is the list SecretProviderClass
                                    names provided by Conjur
                                  items:
                                    properties:
                                      name:
                                        description: Name is the name of the Conjur
                                          SecretProviderClass
                                        type: string
                                      paths:
                                        description: Paths is the list of paths to
                                          the secrets in Conjur
                                        items:
                                          properties:
                                            passwordPath:
                                              description: PasswordPath is the path
                                                to the password in the secret
                                              type: string
                                            usernamePath:
                                              description: UsernamePath is the path
                                                to the username in the secret
                                              type: string
                                          type: object
                                        type: array
                                    type: object
                                  type: array
                                vault:
                                  description: Vault is the list SecretProviderClass
                                    names provided by Vault
                                  items:
                                    type: string
                                  type: array
                              type: object
                            secrets:
                              description: |-
                                Secrets is a collection of kubernetes secrets for storage system credentials
                                Applicable from CSM v1.15 onwards
                                Only one of SecretProviderClasses or Secrets must be specified (mutually exclusive)
                              items:
                                type: string
                              type: array
                            sentinel:
                              description: Sentinel is the name of the sentinel statefulSet
                              type: string
                            skipCertificateValidation:
                              description: skipCertificateValidation is the flag to
                                skip certificate validation
                              type: boolean
                            storageService:
                              description: StorageService is the image tag for the
                                Container
                              type: string
                            storageServiceReplicas:
                              description: StorageServiceReplicas is the number of
                                replicas for storage service deployment
                              type: integer
                            tenantService:
                              description: TenantService is the image tag for the
                                Container
                              type: string
                            tenantServiceReplicas:
                              description: TenantServiceReplicas is the number of
                                replicas for the tenant service deployment
                              type: integer
                            tolerations:
                              description: Tolerations is the list of tolerations
                                for the driver pods
                              items:
                                description: |-
                                  The pod this Toleration is attached to tolerates any taint that matches
                                  the triple <key,value,effect> using the matching operator <operator>.
                                properties:
                                  effect:
                                    description: |-
                                      Effect indicates the taint effect to match. Empty means match all taint effects.
                                      When specified, allowed values are NoSchedule, PreferNoSchedule and NoExecute.
                                    type: string
                                  key:
                                    description: |-
                                      Key is the taint key that the toleration applies to. Empty means match all taint keys.
                                      If the key is empty, operator must be Exists; this combination means to match all values and all keys.
                                    type: string
                                  operator:
                                    description: |-
                                      Operator represents a key's relationship to the value.
                                      Valid operators are Exists, Equal, Lt, and Gt. Defaults to Equal.
                                      Exists is equivalent to wildcard for value, so that a pod can
                                      tolerate all taints of a particular category.
                                      Lt and Gt perform numeric comparisons (requires feature gate TaintTolerationComparisonOperators).
                                    type: string
                                  tolerationSeconds:
                                    description: |-
                                      TolerationSeconds represents the period of time the toleration (which must be
                                      of effect NoExecute, otherwise this field is ignored) tolerates the taint. By default,
                                      it is not set, which means tolerate the taint forever (do not evict). Zero and
                                      negative values will be treated as 0 (evict immediately) by the system.
                                    format: int64
                                    type: integer
                                  value:
                                    description: |-
                                      Value is the taint value the toleration matches to.
                                      If the operator is Exists, the value should be empty, otherwise just a regular string.
                                    type: string
                                type: object
                              type: array
                            vaultConfigurations:
                              description: |-
                                Vaults are the vault configurations
                                Applicable till CSM v1.14
                              items:
                                description: Vault is the configuration for a vault
                                  instance struct
                                properties:
                                  address:
                                    description: Address is the address for this vault
                                    type: string
                                  certificateAuthority:
                                    description: CertificateAuthority is the base64-encoded
                                      certificate authority for validaitng the vault
                                      certificate
                                    type: string
                                  clientCertificate:
                                    description: ClientCertificate is the base64-encoded
                                      certificate for connecting to vault
                                    type: string
                                  clientKey:
                                    description: ClientKey validates is the base64-encoded
                                      certificate key for connecting to vault
                                    type: string
                                  identifier:
                                    description: Identifier is the identifier for
                                      this vault
                                    type: string
                                  role:
                                    description: Role is the role for this vault
                                    type: string
                                  skipCertificateValidation:
                                    description: SkipCertificateValidation validates
                                      the vault server certificate or not
                                    type: boolean
                                type: object
                              type: array
                            volumeMounts:
                              description: VolumeMounts is the list of volume mounts
                                for the container
                              items:
                                description: VolumeMount describes a mounting of a
                                  Volume within a container.
                                properties:
                                  mountPath:
                                    description: |-
                                      Path within the container at which the volume should be mounted.  Must
                                      not contain ':'.
                                    type: string
                                  mountPropagation:
                                    description: |-
                                      mountPropagation determines how mounts are propagated from the host
                                      to container and the other way around.
                                      When not set, MountPropagationNone is used.
                                      This field is beta in 1.10.
                                      When RecursiveReadOnly is set to IfPossible or to Enabled, MountPropagation must be None or unspecified
                                      (which defaults to None).
                                    type: string
                                  name:
                                    description: This must match the Name of a Volume.
                                    type: string
                                  readOnly:
                                    description: |-
                                      Mounted read-only if true, read-write otherwise (false or unspecified).
                                      Defaults to false.
                                    type: boolean
                                  recursiveReadOnly:
                                    description: |-
                                      RecursiveReadOnly specifies whether read-only mounts should be handled
                                      recursively.

                                      If ReadOnly is false, this field has no meaning and must be unspecified.

                                      If ReadOnly is true, and this field is set to Disabled, the mount is not made
                                      recursively read-only.  If this field is set to IfPossible, the mount is made
                                      recursively read-only, if it is supported by the container runtime.  If this
                                      field is set to Enabled, the mount is made recursively read-only if it is
                                      supported by the container runtime, otherwise the pod will not be started and
                                      an error will be generated to indicate the reason.

                                      If this field is set to IfPossible or Enabled, MountPropagation must be set to
                                      None (or be unspecified, which defaults to None).

                                      If this field is not specified, it is treated as an equivalent of Disabled.
                                    type: string
                                  subPath:
                                    description: |-
                                      Path within the volume from which the container's volume should be mounted.
                                      Defaults to "" (volume's root).
                                    type: string
                                  subPathExpr:
                                    description: |-
                                      Expanded path within the volume from which the container's volume should be mounted.
                                      Behaves similarly to SubPath but environment variable references $(VAR_NAME) are expanded using the container's environment.
                                      Defaults to "" (volume's root).
                                      SubPathExpr and SubPath are mutually exclusive.
                                    type: string
                                required:
                                  - mountPath
                                  - name
                                type: object
                              type: array
                          type: object
                        maxItems: 20
                        type: array
                      metrics:
                        description: Metrics configures module-level metrics (e.g.,
                          for resiliency podmon)
                        properties:
                          collection:
                            description: Collection configures metrics collection
                              cadence and cache behavior
                            properties:
                              cacheTTL:
                                description: CacheTTL is how long collected metric
                                  responses are cached
                                type: string
                                x-kubernetes-validations:
                                  - message: cacheTTL must be a non-empty string
                                    rule: self == '' || size(self) > 0
                              interval:
                                description: Interval is how frequently metrics are
                                  collected from the array
                                type: string
                                x-kubernetes-validations:
                                  - message: interval must be a non-empty string
                                    rule: self == '' || size(self) > 0
                            type: object
                          enabled:
                            description: Enabled enables the module metrics endpoint
                            type: boolean
                          podMonitor:
                            description: PodMonitor configures optional Prometheus
                              Operator PodMonitor creation for node pods
                            properties:
                              enabled:
                                description: |-
                                  Enabled creates a PodMonitor CR for Prometheus Operator integration targeting node pods
                                  Requires the Prometheus Operator CRDs to be installed in the cluster
                                type: boolean
                              insecureSkipVerify:
                                description: |-
                                  InsecureSkipVerify skips TLS certificate verification when Prometheus scrapes the node metrics endpoint
                                  Only applies when tlsCertSecret is set
                                type: boolean
                              interval:
                                description: Interval is the Prometheus scrape interval
                                  for the node metrics endpoint
                                type: string
                                x-kubernetes-validations:
                                  - message: interval must be a non-empty string
                                    rule: self == '' || size(self) > 0
                              scrapeTimeout:
                                description: |-
                                  ScrapeTimeout is the Prometheus scrape timeout for the node metrics endpoint
                                  Default value: "" (unset; Prometheus uses its global scrape_timeout)
                                type: string
                                x-kubernetes-validations:
                                  - message: scrapeTimeout must be a non-empty string
                                    rule: self == '' || size(self) > 0
                            type: object
                          port:
                            description: Port is the port on which the metrics endpoint
                              is exposed
                            format: int32
                            type: integer
                          prometheusRule:
                            description: PrometheusRule configures optional Prometheus
                              Operator alert rule creation for supported modules
                            properties:
                              apiErrorRateThreshold:
                                default: 10
                                description: |-
                                  APIErrorRateThreshold is the whole-percent API error threshold that triggers an alert for drivers exposing request success and failure metrics.
                                  PowerScale uses this field.
                                format: int32
                                type: integer
                              apiErrorRateWindow:
                                default: 5m
                                description: |-
                                  APIErrorRateWindow is the lookback window used when evaluating the API error ratio alert expression.
                                  PowerScale uses this field.
                                type: string
                              applianceCriticalThreshold:
                                default: 90
                                description: |-
                                  ApplianceCriticalThreshold is the whole-percent appliance utilization threshold that triggers a critical alert for drivers exposing appliance metrics.
                                  PowerStore uses this field.
                                format: int32
                                type: integer
                              applianceWarningThreshold:
                                default: 80
                                description: |-
                                  ApplianceWarningThreshold is the whole-percent appliance utilization threshold that triggers a warning alert for drivers exposing appliance metrics.
                                  PowerStore uses this field.
                                format: int32
                                type: integer
                              connectivitySuccessRatio:
                                default: 90
                                description: |-
                                  ConnectivitySuccessRatio is the minimum percentage of successful array connectivity checks
                                  below which the RES-02 CSMResiliencyConnectivityLost alert fires.
                                  This field is only read when set under spec.modules[name=resiliency].metrics.prometheusRule.
                                  Setting it under spec.driver.metrics.prometheusRule has no effect on resiliency alerts.
                                format: int32
                                maximum: 100
                                minimum: 1
                                type: integer
                              cpuWarningThreshold:
                                default: 80
                                description: |-
                                  CPUWarningThreshold is the whole-percent CPU utilization threshold that triggers a warning alert for driver pods.
                                  PowerScale uses this field.
                                format: int32
                                type: integer
                              driverCrashLoopRestartThreshold:
                                default: 3
                                description: |-
                                  DriverCrashLoopRestartThreshold is the restart count threshold used to detect crash looping within the configured window.
                                  PowerScale uses this field.
                                format: int32
                                type: integer
                              driverCrashLoopWindow:
                                default: 15m
                                description: |-
                                  DriverCrashLoopWindow is the lookback window used when evaluating crash loop restart deltas.
                                  PowerScale uses this field.
                                type: string
                              enabled:
                                default: true
                                description: Enabled creates a PrometheusRule CR for
                                  Prometheus Operator integration.
                                type: boolean
                              memoryWarningThreshold:
                                default: 2147483648
                                description: |-
                                  MemoryWarningThreshold is the driver memory usage threshold in bytes that triggers a warning alert.
                                  PowerScale uses this field.
                                format: int64
                                type: integer
                              nfsV3LatencyWarningSeconds:
                                default: 10
                                description: |-
                                  NFSv3LatencyWarningSeconds is the NFSv3 p99 latency threshold in seconds that triggers a warning alert.
                                  PowerScale uses this field.
                                format: int32
                                type: integer
                              nfsV4LatencyWarningSeconds:
                                default: 10
                                description: |-
                                  NFSv4LatencyWarningSeconds is the NFSv4 p99 latency threshold in seconds that triggers a warning alert.
                                  PowerScale uses this field.
                                format: int32
                                type: integer
                              nodepoolCriticalThreshold:
                                default: 90
                                description: |-
                                  NodepoolCriticalThreshold is the whole-percent node pool utilization threshold that triggers a critical alert for drivers exposing node pool metrics.
                                  PowerScale uses this field.
                                format: int32
                                type: integer
                              nodepoolWarningThreshold:
                                default: 80
                                description: |-
                                  NodepoolWarningThreshold is the whole-percent node pool utilization threshold that triggers a warning alert for drivers exposing node pool metrics.
                                  PowerScale uses this field.
                                format: int32
                                type: integer
                              quotaCriticalThreshold:
                                default: 90
                                description: |-
                                  QuotaCriticalThreshold is the whole-percent quota utilization threshold that triggers a critical alert for drivers exposing quota metrics.
                                  PowerScale uses this field.
                                format: int32
                                type: integer
                              quotaWarningThreshold:
                                default: 80
                                description: |-
                                  QuotaWarningThreshold is the whole-percent quota utilization threshold that triggers a warning alert for drivers exposing quota metrics.
                                  PowerScale uses this field.
                                format: int32
                                type: integer
                              srpSnapshotCapacityWarning:
                                default: 80
                                description: |-
                                  SRPSnapshotCapacityWarning is the whole-percent SRP snapshot capacity threshold that triggers
                                  a warning alert. PowerMax uses this field (PM-15). SRP-level monitoring is the permanent
                                  solution (per-SG snapshot metric is not required per product management decision).
                                format: int32
                                type: integer
                              storageGroupCapacityCritical:
                                default: 90
                                description: |-
                                  StorageGroupCapacityCritical is the whole-percent Storage Group utilization threshold that
                                  triggers a critical alert. PowerMax uses this field (PM-14).
                                format: int32
                                type: integer
                              storageGroupCapacityWarning:
                                default: 80
                                description: |-
                                  StorageGroupCapacityWarning is the whole-percent Storage Group utilization threshold that
                                  triggers a warning alert. PowerMax uses this field (PM-13).
                                format: int32
                                type: integer
                              volumeOperationFailureThreshold:
                                default: 3
                                description: |-
                                  VolumeOperationFailureThreshold is the cumulative failure count threshold for volume operations
                                  (CreateVolume, DeleteVolume, ControllerPublishVolume, ControllerUnpublishVolume,
                                  NodeStageVolume, NodeUnstageVolume, NodePublishVolume, NodeUnpublishVolume) that triggers a
                                  warning alert. PowerMax uses this field (PM-01 to PM-08). PowerStore uses this field.
                                format: int32
                                type: integer
                            type: object
                          serviceMonitor:
                            description: ServiceMonitor configures optional Prometheus
                              Operator ServiceMonitor creation
                            properties:
                              enabled:
                                description: |-
                                  Enabled creates a ServiceMonitor CR for Prometheus Operator integration
                                  Requires the Prometheus Operator CRDs to be installed in the cluster
                                type: boolean
                              insecureSkipVerify:
                                description: |-
                                  InsecureSkipVerify skips TLS certificate verification when Prometheus scrapes the metrics endpoint
                                  Only applies when tlsCertSecret is set
                                type: boolean
                              interval:
                                description: Interval is the Prometheus scrape interval
                                  for the metrics endpoint
                                type: string
                                x-kubernetes-validations:
                                  - message: interval must be a non-empty string
                                    rule: self == '' || size(self) > 0
                              scrapeTimeout:
                                description: |-
                                  ScrapeTimeout is the Prometheus scrape timeout for the metrics endpoint
                                  Default value: "" (unset; Prometheus uses its global scrape_timeout)
                                type: string
                                x-kubernetes-validations:
                                  - message: scrapeTimeout must be a non-empty string
                                    rule: self == '' || size(self) > 0
                            type: object
                          tlsCertSecret:
                            description: |-
                              TLSCertSecret is the name of a Kubernetes TLS Secret (tls.crt / tls.key) used to serve metrics over HTTPS
                              When set the operator mounts the secret and configures HTTPS automatically
                            type: string
                        type: object
                      name:
                        description: Name is name of ContainerStorageModule modules
                        type: string
                    type: object
                  maxItems: 20
                  type: array
                retainImageRegistryPath:
                  description: RetainImageRegistryPath is the boolean flag used to
                    retain image registry path
                  type: boolean
                upgrade:
                  description: |-
                    Upgrade is the upgrade policy for the CSM CR.
                    When set to "auto", the operator will automatically upgrade the CSM resource
                    to the latest supported version.
                    If the auto upgrade fails, the operator will automatically rollback to the
                    previous version.
                  enum:
                    - auto
                    - manual
                    - ""
                  type: string
                version:
                  type: string
              type: object
              x-kubernetes-validations:
                - message: spec.version and spec.driver.configVersion cannot both
                    be set
                  rule: "!(has(self.version) && self.version != \"\" && has(self.driver)
                    && has(self.driver.configVersion) && self.driver.configVersion
                    != \"\")"
                - message: spec.driver.common.image is forbidden when spec.version
                    is set
                  rule: "!(has(self.version) && self.version != \"\" && has(self.driver)
                    && has(self.driver.common) && has(self.driver.common.image) &&
                    self.driver.common.image != \"\")"
                - message: spec.driver.sideCars[*].image is forbidden when spec.version
                    is set
                  rule: "!(has(self.version) && self.version != \"\" && has(self.driver)
                    && has(self.driver.sideCars) && self.driver.sideCars.exists(sc,
                    has(sc.image) && sc.image != \"\"))"
                - message: spec.modules[*].components[*].image is forbidden when spec.version
                    is set
                  rule: "!(has(self.version) && self.version != \"\" && has(self.modules)
                    && self.modules.exists(m, has(m.components) && m.components.exists(c,
                    has(c.image) && c.image != \"\")))"
                - message: spec.customRegistry is forbidden when spec.version is empty
                  rule: "!(has(self.customRegistry) && self.customRegistry != \"\"
                    && !(has(self.version) && self.version != \"\"))"
                - message: spec.retainImageRegistryPath is forbidden unless both spec.version
                    and spec.customRegistry are set
                  rule: "!(has(self.retainImageRegistryPath) && !(has(self.version)
                    && self.version != \"\" && has(self.customRegistry) && self.customRegistry
                    != \"\"))"
                - message: spec.driver.initContainers[*].image is forbidden when spec.version
                    is set
                  rule: "!(has(self.version) && self.version != \"\" && has(self.driver)
                    && has(self.driver.initContainers) && self.driver.initContainers.exists(ic,
                    has(ic.image) && ic.image != \"\"))"
                - message: env NGINX_PROXY_IMAGE is forbidden when spec.version is
                    set
                  rule: "!(has(self.version) && self.version != \"\" && has(self.modules)
                    && self.modules.exists(m, has(m.components) && m.components.exists(c,
                    has(c.envs) && c.envs.exists(e, has(e.name) && e.name == \"NGINX_PROXY_IMAGE\"))))"
                - message: spec.driver.metrics is only supported for PowerFlex, PowerScale,
                    PowerStore, and PowerMax drivers
                  rule: "!has(self.driver) || !has(self.driver.metrics) || self.driver.csiDriverType
                    == 'powerflex' || self.driver.csiDriverType == 'isilon' || self.driver.csiDriverType
                    == 'powerstore' || self.driver.csiDriverType == 'powermax'"
                - message: spec.driver.common.image is forbidden when spec.driver.configVersion
                    is set
                  rule: "!(has(self.driver) && has(self.driver.configVersion) && self.driver.configVersion
                    != \"\" && has(self.driver.common) && has(self.driver.common.image)
                    && self.driver.common.image != \"\")"
                - message: spec.driver.sideCars[*].image is forbidden when spec.driver.configVersion
                    is set
                  rule: "!(has(self.driver) && has(self.driver.configVersion) && self.driver.configVersion
                    != \"\" && has(self.driver.sideCars) && self.driver.sideCars.exists(sc,
                    has(sc.image) && sc.image != \"\"))"
                - message: spec.driver.initContainers[*].image is forbidden when spec.driver.configVersion
                    is set
                  rule: "!(has(self.driver) && has(self.driver.configVersion) && self.driver.configVersion
                    != \"\" && has(self.driver.initContainers) && self.driver.initContainers.exists(ic,
                    has(ic.image) && ic.image != \"\"))"
                - message: spec.modules[*].components[*].image is forbidden when spec.driver.configVersion
                    is set
                  rule: "!(has(self.driver) && has(self.driver.configVersion) && self.driver.configVersion
                    != \"\" && has(self.modules) && self.modules.exists(m, has(m.components)
                    && m.components.exists(c, has(c.image) && c.image != \"\")))"
                - message: spec.modules[*].initContainer[*].image is forbidden when
                    spec.driver.configVersion is set
                  rule: "!(has(self.driver) && has(self.driver.configVersion) && self.driver.configVersion
                    != \"\" && has(self.modules) && self.modules.exists(m, has(m.initContainer)
                    && m.initContainer.exists(ic, has(ic.image) && ic.image != \"\")))"
            status:
              description: ContainerStorageModuleStatus defines the observed state
                of ContainerStorageModule
              properties:
                controllerStatus:
                  description: ControllerStatus is the status of Controller pods
                  properties:
                    available:
                      description: Available is the number of available pods
                      type: string
                    desired:
                      description: Desired is the number of desired pods
                      type: string
                    failed:
                      description: Failed is the number of failed pods
                      type: string
                  type: object
                lastSuccessfulConfiguration:
                  description: LastSuccessfulConfiguration is configurations details
                    only when the CSM CR goes into a successful state
                  type: string
                nodeStatus:
                  description: NodeStatus is the status of Controller pods
                  properties:
                    available:
                      description: Available is the number of available pods
                      type: string
                    desired:
                      description: Desired is the number of desired pods
                      type: string
                    failed:
                      description: Failed is the number of failed pods
                      type: string
                  type: object
                state:
                  description: State is the state of the driver installation
                  type: string
              type: object
          type: object
      served: true
      storage: true
      subresources:
        status: {}
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: null
  storedVersions: null
